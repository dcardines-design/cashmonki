# CashMonki Savepoint — 2026-07-04 16:27 (feedback board round 2 + homepage card + backend outage)

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI "CashMonki"). iOS 18, Xcode open, user runs ⌘R (no CLI build). **UI changes need approval; use existing components.** Verify: `xcrun swiftc -parse <file>` (SourceKit inline "cannot find X" = isolated-parse noise — IGNORE). Continues auto_2026-07-02_18-55.md.

## CRITICAL: receipt upload outage — billing disabled
Receipt analysis is 100% down since ≥Jun 7. `firebase functions:log --project cashmonki-app` shows only "The request failed because billing is disabled for this project"; endpoint `https://us-central1-cashmonki-app.cloudfunctions.net/api/api/app-config` returns 503 HTML. Cloud Functions v2 needs Blaze. **User tried to re-add billing account and was BLOCKED** — gave step-by-step (console.cloud.google.com/billing → show closed accounts → reopen/pay → link project cashmonki-app #817492386994, NOT `cashmonki`/`cashooya`). Unresolved; user to report exact error. Verify with curl when they say "try again". firebase CLI is authed (`npx firebase-tools`). Stopgap offered (not built): fall back to legacy direct-OpenRouter `analyzeReceipt` in AIReceiptAnalyzer.swift (key via keychain/env/Info.plist).

## Changes since last savepoint (all parse-clean; user still hasn't rebuilt/verified ANY of it)
1. **@ tagging (structured)**: FeedbackComment gained `replyToHandle: String?` (init param after `text`). addReply resolves target (incl. flattened reply-to-reply), skips self-reply, strips user-typed "@handle" prefix from text. Rendering: `commentText(for:)` in FeedbackComponents builds blue tag from field (old leading-@ string parser deleted). Seed reply uses field + clean text. Composer prepend hack removed. No persistence → no migration.
2. **Header truncation**: post sheet header = title capped 22 chars + "…" (`headerTitle(for:)`); ≤22 shows no ellipsis — flagged to user (mock shows … at 19 chars), they didn't object.
3. **Empty subtext collapses**: post sheet + inbox card hide desc Text when detail is whitespace-empty.
4. **Homepage feedback promo card** (`feedbackPromoCard` in HomePageComponents, placed after `actionTiles` in mainContentStack): VStack(spacing 4) "Got feedback?" semibold16 white / subtitle medium12 Color(.66,.66,.66); pad h20/v16; bg Color(0.15,0.15,0.15); cornerRadius(10); art `feedback-card-art` imageset (@2x, 446×142 → 223×71pt, from ~/Downloads/"feedback card image.png") ZStack-trailing in background. Tap → `showingFeedbackBoard` state → .slideInSheet FeedbackInboxSheet (added in bodyWithSheets, HomePage.swift).
5. **AppInputField.multiline(...)**: new `isMultiline/minLines(3)/maxLines(8)` init params (before externalFocusBinding); TextField(axis:.vertical).lineLimit(min...max); placeholder overlay top-aligns via conditional Spacer in VStack. AddFeedbackSheet Description switched to it.
6. **Admin = dcardinesiii@gmail.com**: FeedbackManager `adminEmails` set + computed `isAdmin` (email match OR long-press `isAdminMode` toggle). All feedback UI reads switched isAdminMode→isAdmin (PostSheet 3 sites, InboxSheet 3 sites, canModifyComment, currentUserIsCreator). Toggle writes unchanged. NOTE: isAdmin not @Published — no live refresh on login change.
7. **"See more"**: post body lineLimit 3 ↔ nil; ViewThatFits(.vertical) background trick sets `isPostTruncated` (only shows button when actually >3 lines); AppButton.text "See more"/"See less" .tripleExtraSmall .fixedSize.
8. **Mario hop**: UpvoteButton keyframeAnimator(trigger: jumpCount) offset −8 CubicKeyframe(0.12s) → SpringKeyframe(0, 0.35, .bouncy); `handleTap` hops only when liking, both styles (Group wrapper).

## Earlier this session (see 07-02 savepoints for detail)
Filled thumb asset; Comment=AppButton .secondary; outline message-square-01 (asset SVG replaced — was solid); post buttons .tripleExtraSmall (UpvoteButton `boxedSize` param, card unchanged); comments white r16 tile on surfacePrimary (inset 12, pad h24/v22, gaps 28, 1px thread line, hidden when empty, text→icons 12); action sheet = 3 AppButtons (Edit/Delete HIDDEN unless owner; admin sees Delete; Delete ALWAYS red — user later confirmed admin delete), dynamic detent buttons*44+(n−1)*16+66; AppButton gained `textColorOverride` (+ pass `isEnabled:` param, .disabled() env NOT read).

## UX/permission flaws flagged to user (fixes NOT requested yet; priority order given)
1. Long-press admin backdoor now a real privilege hole (remove or #if DEBUG) 2. delete has no confirmation 3. author can't see own pending post (submit → void), no submit toast, no upvote sort 4. can delete but not edit own post 5. email-prefix identity collisions (canModifyComment matches handles) 6. all enforcement client-side; admin email must become Firebase custom claim + security rules at Firestore migration.

## Also answered
- Sim login with dcardinesiii@gmail.com works (Firebase Auth is free-tier, unaffected by billing outage); email now grants feedback admin (change #6).
- figma-desktop local MCP works (live selection); user drives with CleanShot measurement screenshots + pasted SwiftUI specs.

## Next steps
1. USER: rebuild ⌘R and visually verify the entire 2-session backlog (incl. Roman font rebuild).
2. USER: fix billing (blocked on adding account) → then curl-verify backend.
3. Offered/undecided: UX flaw fixes above; composer TextField→AppInputField.multiline swap now trivial; DebugSettingsView unreachable admin row removal.
