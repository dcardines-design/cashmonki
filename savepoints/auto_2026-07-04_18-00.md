# CashMonki Savepoint — 2026-07-04 18:00 (UX-flaw fixes round + caveman + Release-scheme discovery)

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI). User runs ⌘R. UI changes need approval; use existing components. Verify: `xcrun swiftc -parse` (SourceKit inline noise = IGNORE). Continues auto_2026-07-04_16-27.md. **CAVEMAN MODE ACTIVE** (full) — user installed github:JuliusBrussee/caveman plugin, said "caveman mode"; terse replies until "normal mode". Task list in harness tracks open work (#5,6,7,8,9 pending).

## CRITICAL DISCOVERY: scheme runs RELEASE
Xcode scheme "Cashooya Playground" LaunchAction was `Release` → all `#if DEBUG` stripped from user's ⌘R sim builds. I edited the .xcscheme to Debug on disk but Xcode DIDN'T reload it (user's next build still Release; DerivedData only has Release-iphonesimulator, binary rebuilt 17:51). Workaround shipped: admin-preview gates now `#if DEBUG || targetEnvironment(simulator)` — works in any sim build, never on device release. Scheme file on disk now says Debug for Run; user may see Xcode prompt to reload.

## This round's changes (parse-clean; toggle NOT yet visually confirmed)
1. **Role column (flaw #1)**: UserData gained `role: String?` ("user" default, optional=back-compat) + `isAdmin` computed. UserManager: `adminEmails = [dcardinesiii@gmail.com]`, `ensureRoleAssigned()` (sets role by email + syncToFirebase) called after local load (restoreUserSession) + Firebase mergedUser. FeedbackManager.isAdmin = role check + sim/debug-only isAdminMode toggle.
2. **Delete toasts (flaw #2, user chose no-confirm)**: post/comment delete → ripped-paper `toastManager.showDeleted("Post deleted"/"Comment deleted")` (ToastManager = UIWindow-based, shows above fullScreenCover; @EnvironmentObject in FeedbackPostSheet).
3. **Submit toast (task 1)**: AddFeedbackSheet → showSuccess("Feedback submitted!"). Pending-post visibility ALREADY existed (inbox line ~37).
4. **Email identity (task 2)**: FeedbackItem/FeedbackComment `authorHandle` stored field → `authorEmail` (full lowercase, never displayed) + computed `authorHandle` via new global `feedbackHandle(fromEmail:)` in FeedbackModels. FeedbackManager: `currentUserEmail` (lowercase), canModifyComment(authorEmail:), addReply targets/self-skip by email. Seeds use @example.com. All view compare-sites swapped.
5. **Edit own post (task 3)**: FeedbackManager.editFeedback(itemID:title:detail:); AddFeedbackSheet gained `editingItem: FeedbackItem?` (custom init prefills @State; header "Edit Feedback"; button "Save changes"; toast "Feedback updated!"); post ⋮ Edit for owner → `showingEditPost` slideInSheet in FeedbackPostSheet; actionSheetHeight post-branch canEdit updated.
6. **Inbox sort (task 4)**: Open = upvotes desc, ties newest; New/Done = newest first.
7. **Admin preview toggle**: long-press → triple-tap (failed, Release build) → now visible Toggle overlay trailing in inbox SheetHeader, tint accentBackground, scale 0.7, haptic, leaves New tab on disable. Gated sim/debug (see above).
8. **Thumb hop faster**: -8pt CubicKeyframe 0.08s + SpringKeyframe 0.22s .snappy.
9. **Feedback promo card**: vertical padding 16→15 (-2pt height).
10. **Caveman installer broke build**: wrote skills into CashMonki/.agents (synced folder) → "Multiple commands produce SKILL.md". Moved to repo root `cashooya-playground/.agents/`. Fixed.

## Task list state
Done: #1-4. Pending: **#5** email notify (Vercel+Resend plan approved-ish, never got explicit go; needs RESEND_API_KEY decision), **#6** billing (user blocked adding billing account; steps given; verify curl app-config after), **#7** user rebuild+verify entire backlog, **#8** composer → AppInputField.multiline + remove dead DebugSettingsView admin row, **#9** Firestore migration (rules, custom claim, replyToUserID).

## Notes
- AppInputField.multiline exists (isMultiline/minLines/maxLines params; placeholder top-aligns).
- AppButton: textColorOverride, isEnabled param (not .disabled() env).
- figma-desktop MCP live-selection works; user pastes CleanShot specs.
- Caveman skills at ~/.claude/plugins/cache/caveman; /caveman commands after CC restart.
