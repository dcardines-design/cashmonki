# Savepoint — CashMonki — 2026-07-18 17:23

## Build/env
App `/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Build REQUIRES `CODE_SIGNING_ALLOWED=NO`. Version 3.6.0 / build 3. SourceKit cross-file errors = FALSE POSITIVES. Active sim = **A67ED167** (iPhone 16 Pro Max). DerivedData `CashMonki-gefiakeveutzgnaaobjcdlxkkdxx` (shared by Xcode + my simctl builds).

## Git
- Branch `sync-cleanup-and-dataloss-guardrails` @ `5871cf4`.
- Branch `firestore-source-of-truth` @ `a7a1db5` (current). **LOTS UNCOMMITTED** on it: today's crisis fixes + onboarding monotonic guard + recovery-dump disable + the Figma sync card + carousel. Commit when user OKs the card.
- Not pushed.

## KEY GOTCHA discovered — "changes not applying"
DerivedData module cache got CORRUPTED: `fatal error: module file 'Foundation-...pcm' not found` → builds SILENTLY FAILED → stale app kept running with none of my edits. This is why the user kept not seeing changes. FIX: `rm -rf ~/Library/Developer/Xcode/DerivedData/CashMonki-gefiakeveutzgnaaobjcdlxkkdxx/Build/Intermediates.noindex/ExplicitPrecompiledModules` + `xcodebuild ... clean build`. **User must Shift+Cmd+K (Clean Build Folder) then Cmd+R in Xcode** — their Xcode has the same corrupt cache. Also: user runs via Xcode; my simctl install/launch to A67ED167 gets overwritten when they Cmd+R. Whichever launched last is what shows.

## Crisis fixes this session (all built, uncommitted)
1. **Blank white screen on launch** — my Firestore Phase 1 turned offline persistence ON unconditionally at launch → hung. FIX: gated persistence config + `_ = FirestoreStore.shared` behind `FeatureFlags.firestoreSourceOfTruth` (default OFF) in CashMonkiApp.swift. Default build = old behavior.
2. **Onboarding restarting on a completed account** — box `currentUser_firebase_ZalijpiofgQlumznBT0JcSmcms12` (641 txns, "Dante Cardines", real categories, admin) had `onboardingCompleted` downgraded 16→2 by a launch recompute reading wiped global flags. FIX: `UserManager.updateOnboardingProgress(_:allowDowngrade:Bool=false)` now MONOTONIC (refuses to lower unless allowDowngrade:true; only last-wallet-delete passes true). VERIFIED self-healed back to 16 on relaunch (checkAndMarkOnboardingComplete raises when 641 txns + 2-word name + default wallet = substantial usage). App Check unregistered on A67ED167 makes Firebase config slow → restore/heal runs late; read box AFTER ~10s.
3. **Disabled recovery-dump leftovers** — `dumpUserStorageForRecovery()` + `restoreDanteData()` (2026-07-08 tools) commented out in CashMonkiApp.swift (~L51-52); re-ran every launch, dumped 530KB, drowned logs.

## Figma sync card + carousel (Home) — IN PROGRESS, just finished art
- User wants "Save data to cloud" card under the feedback bar, in a CAROUSEL like subscription cards. Figma node 1694-7193, file `ranVMXtnIZLnq6W5FVXxt6`.
- MEMORY `feedback_figma_specs_manual` said don't call get_design_context — but user EXPLICITLY asked to use the Figma link/graphic + "do you see the figma mcp?" So I USED the Figma MCP (get_design_context + download_assets). Update: for THIS card user wanted MCP used.
- Exact Figma spec: bg `#87eaff` = Color(0.529,0.918,1.0); title "Save data to cloud" Overused Grotesk SemiBold 16 black; subtitle "Log in to sync and secure your data!" Medium 12 `#01677c`=Color(0.004,0.404,0.486); radius 10; padding top12/bottom16/px20. Art = cloud + receipt/paper (rotated) + green refresh-ccw icon + gray dashed lines, on the RIGHT.
- Downloaded real art PNGs → `CashMonki/Assets.xcassets/sync-cloud.imageset` (240x78) + `sync-receipt.imageset` (279x191). (First attempt I wrongly recreated with SF Symbols — user called it out; now uses real assets.)
- Files: `CashMonki/Pages/HomePageComponents.swift` — `promoCarousel` (pages [.sync,.feedback] when !isAuthenticated, else [.feedback]; horizontal paged ScrollView + peek + dots, mirrors subscription carousel), `syncPromoCard` (real art, exact colors, tap→showingConnectAccount), `PromoPage` enum, `promoView(for:)`. `CashMonki/Pages/HomePage.swift` — added `@State showingConnectAccount`, `@ObservedObject authManager = AuthenticationManager.shared`, `@State currentPromoPage`, replaced feedbackPromoCard with `promoCarousel` in mainContentStack (~L429), added `.sheet(isPresented:$showingConnectAccount){ LoginView(onLogin:{...},onShowRegister:{},allowGuest:false) }`.
- Card LOGIC confirmed w/ user: show for guests/not-logged-in, hide when logged in. Current sim state: isAuthenticated=false (Firebase keychain broken on sim, anon sign-in failed) → card shows.
- Built + installed + launched. AWAITING user to view (must Clean+Run in Xcode) and confirm art placement (cloud offset x-2/y-10 w84, receipt x-16/y6 w104 — approximate, may need nudging).

## Next steps
1. User: Clean Build Folder + Cmd+R in Xcode to see the card (their cache is corrupt).
2. Tune cloud/receipt art offsets if placement off.
3. COMMIT: today's crisis fixes + onboarding guard + recovery-dump disable + Figma card/carousel (firestore branch). User asked to commit when happy with card.
4. Firestore migration Phase 2 (write path) still pending — flag-gated. Plan artifact: https://claude.ai/code/artifact/af206a68-195c-4614-aebc-fcf4f0923e02. Decisions locked: ask-chat single-thread per-message docs; receipt imgs Firestore→Storage later; wallet unify AccountData+SubAccount (preserve ids); security rules auth.uid==uid w/ Phase1; legacy stores reconcile→verify→delete-later.
5. Cross-device sync answer given: works only for LOGGED-IN same account (login-time merge + delta, not live); anonymous = single device. Sim currently can't sync (App Check debug token unregistered on A67ED167 + no security rules).
6. Still user-side: RevenueCat transfer behavior → "Transfer to new App User ID"; Firestore security rules paste; register A67ED167's App Check debug token; re-archive 3.6.0(3) for TestFlight.

## User prefs (persist)
Caveman mode FULL. Data PRECIOUS. Figma: for design-implement tasks user WANTS the Figma MCP used (get_design_context + download_assets) to pull real assets — don't recreate with SF Symbols. UI changes need approval (card was requested=approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose. Xcode Run wipes sim container each run.
