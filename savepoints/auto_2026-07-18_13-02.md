# Savepoint — CashMonki — 2026-07-18 13:02

## Build/env
App `/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Build REQUIRES `CODE_SIGNING_ALLOWED=NO`. Version 3.6.0 / build 3. SourceKit cross-file errors = FALSE POSITIVES. All builds SUCCEEDED. Active sim now **A67ED167** (iPhone 16 Pro Max) — its App Check debug token is NOT registered (only 2E00's `sim-xcode`/`sim-xcode-2` are), so Firebase config is slow there; not fatal.

## Git
- Branch `sync-cleanup-and-dataloss-guardrails` @ `5871cf4` — all sync cleanup + category fixes + data-loss guardrails.
- Branch `firestore-source-of-truth` @ `a7a1db5` (current) — Firestore migration Phase 1 (offline persistence + read repos, flag-off).
- **UNCOMMITTED on firestore branch** (today's crisis fixes — need commit): CashMonkiApp.swift, UserManager.swift, OnboardingStateManager.swift.
- Not pushed. `build_dd/` gitignored.

## What happened this session (after Phase 1 commit)
1. **Blank white screen on launch** — ROOT: my Phase 1 turned Firestore offline persistence ON unconditionally at launch → hung. FIX: gated the persistence config + `_ = FirestoreStore.shared` behind `FeatureFlags.firestoreSourceOfTruth` (default OFF) in CashMonkiApp.swift. Default build now = old behavior. Blank resolved.
2. **Onboarding started on a completed account** — ROOT: box `currentUser_firebase_ZalijpiofgQlumznBT0JcSmcms12` (641 txns, name "Dante Cardines", good data) had `onboardingCompleted` DOWNGRADED 16→2 by a launch-time recompute path that reads wiped global boolean flags. `setOnboardingProgress` guards downgrades but other paths call low-level `updateOnboardingProgress` directly. FIX: made `UserManager.updateOnboardingProgress(_:allowDowngrade:Bool=false)` MONOTONIC — refuses to lower unless `allowDowngrade:true`. Only explicit reset (last-wallet-deleted, UserManager:~2314) passes `allowDowngrade:true`. Migration (OnboardingStateManager:421) stays monotonic. VERIFIED: box self-healed back to 16 on relaunch (restoreUserSession→checkAndMarkOnboardingComplete raises 2→16; 641 txns + 2-word name + default wallet = substantial usage), guard blocks re-downgrade.
3. **Disabled 530KB recovery-dump leftovers** — `dumpUserStorageForRecovery()` + `restoreDanteData()` (2026-07-08 incident tools) commented out in CashMonkiApp.swift (~line 51-52); re-ran every launch, drowned logs.

## Data status
GOOD data is intact on sim A67ED167: box ZalijpiofgQ, 641 transactions, real categories (Food/Coffee/Shopping/etc), onboardingCompleted=16, name "Dante Cardines", admin. This is the user's real dataset. Earlier category "No Category" issue was a DIFFERENT sim/state; this box's categories resolve fine.

## Log-capture note
App `print()` goes to stdout — capture via `xcrun simctl launch --console <SIM> com.dante.cashmonki >> file 2>&1 &` then wait (Firebase config slow on A67ED167). `log show` only gets os_log (Firebase/AppCheck), not print(). Reading the box directly: `simctl get_app_container <SIM> com.dante.cashmonki data` → `Library/Preferences/com.dante.cashmonki.plist` → decode `currentUser_firebase_<uid>` bytes as JSON.

## IN PROGRESS — new task (user's last msg): Figma sync-prompt card
User wants a "data sync prompt bar card" placed UNDER the feedback bar on Home, from Figma node 1694-7193 (link: figma.com/design/ranVMXtnIZLnq6W5FVXxt6/Cashmonki?node-id=1694-7193). Provided the card ART image (light-blue cloud + green refresh/sync icon, dashed receipt line).
- Insertion point FOUND: right after `feedbackPromoCard` in `CashMonki/Pages/HomePageComponents.swift:123` (dark card, art-on-side pattern; feedback art asset = "feedback-card-art").
- MEMORY `feedback_figma_specs_manual`: user PASTES exact SwiftUI specs; DON'T call Figma get_design_context. So I asked user for: copy (title/subtitle/CTA), style values (bg/radius/padding/colors), and the art asset export (or "recreate in SwiftUI"). AWAITING their spec or a "go" to build a first pass.
- The card = nudge guests/local users to enable cloud sync (ties to existing "Sync Data Online" feature; tap → connect/sync flow).

## Next steps
1. Commit today's 3 crisis fixes (persistence gating, onboarding monotonic guard, recovery-dump disable) — user asked whether to commit before the card; pending their yes.
2. Build the Figma sync card once user pastes spec / says go. Place after feedbackPromoCard.
3. Firestore migration Phase 2 (write path) still pending — flag-gated. Plan artifact: https://claude.ai/code/artifact/af206a68-195c-4614-aebc-fcf4f0923e02
4. Locked migration decisions: ask-chat=single thread per-message docs; receipt imgs=Firestore now→Storage later; wallet=unify AccountData+SubAccount (preserve ids, all-cases enum, decode-with-defaults); security rules auth.uid==uid ship w/ Phase 1; legacy stores reconcile→verify→delete-later.
5. Still open (user-side): RevenueCat transfer behavior → "Transfer to new App User ID"; Firestore security rules paste; re-archive 3.6.0(3) for TestFlight.

## User prefs (persist)
Caveman mode FULL. Data PRECIOUS — no destructive auto-mutation; onboarding now monotonic. UI changes need approval (this card IS requested = approved). Figma: user pastes specs, don't auto-call get_design_context. Never `sed -i` in-place. CLAUDE.md/Info.plist hold secrets — never commit/expose. Testing via Xcode Run wipes sim container each run.
