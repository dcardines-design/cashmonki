# CashMonki Savepoint — 2026-07-02 17:55

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI "CashMonki"). iOS 18, local git, Xcode open, no CLI build (user runs ⌘R). **UI changes need approval.** Figma MCP connected (file key `ranVMXtnIZLnq6W5FVXxt6`). Verify edits with `xcrun swiftc -parse` (SourceKit "cannot find AppColors/UIKit" = whole-module/macOS-SDK noise, ignore). Feedback screenshots: get_screenshot → curl URL to scratchpad → Read; download_assets(svg) exports node WITH frame context (extract inner `<g id=...>` path).

## This session (continues auto_2026-07-02_17-32.md)

### Figma status chips (earlier) — DONE
Components/Chip.swift (reusable: HStack spacing4, pad h8/v6, radius4). AppColors got statusDone/InProgress/Pending tokens. FeedbackStatus uses them.

### Untitled UI icons — DONE
- Exported SVGs from Figma frame 1607-6161, extracted inner paths, built imagesets in `CashMonki/Assets.xcassets`: **thumbs-up, message-square-01, tool-01, check** (+ reuse existing 24×24 `clock` in DesignSystem/Assets — do NOT duplicate; used by Settings tab group too).
- User later dropped canonical `~/Downloads/{tool-01,check,clock}.svg`; copied tool-01+check into imagesets, clock reused existing.
- All icons render via **AppIcon(assetName:fallbackSystemName:size:)** (template → recolored by foreground). Verified rendering via `qlmanage -t`.
- Chip.swift now takes iconAsset + iconFallback (SF). FeedbackStatus.iconAsset = clock/tool-01/check; systemIcon = SF fallback.

### Upvote button → existing component — DONE
UpvoteButton.boxed now uses **AppButton(hierarchy:.secondary, size:.extraSmall, leftIcon:"thumbs-up", iconColorOverride: upvoted ? accentBackground : foregroundPrimary)** + .fixedSize(). Inline style = AppIcon thumbs-up + count. NOTE: AppButton.secondary title color not overridable → upvoted COUNT stays black, only thumb turns purple (told user).

### Inbox tweaks — DONE
FeedbackInboxSheet: tabs top padding 4→20; "Add feedback" now AppButton.secondary + iconColorOverride black (bordered white per Figma), divider removed, bottom bar bg surfacePrimary.

### FeedbackCard redesign (dev-mode specs) — DONE
- **Status chip TOP-LEFT**, upvote TOP-RIGHT (same HStack top row).
- Title+description = left-column VStack(spacing:8); desc lineLimit(2) constrained to left column.
- Footer = meta line ONLY (status pill removed from footer, moved to top).
- padding 20; outer VStack spacing 24 (block→footer); cornerRadius 14.

### FeedbackAuthorLine redesign — DONE
avatar · handle · creator · • · time · • · 💬count. spacing **8**; dot separators = `Circle().fill(AppColors.line1stLine).frame(2,2)` (user gave exact styling); optional `commentCount` param (card passes it, post-detail/comment-rows omit → nil).

All in Components/FeedbackComponents.swift, Components/Chip.swift, Models/FeedbackModels.swift, Sheets/FeedbackInboxSheet.swift, Sheets/FeedbackPostSheet.swift. Parses clean.

## Standing user directives (IMPORTANT)
- **"Use existing components always instead of creating from scratch."** Prefer AppButton/AppInputField/AppIcon/TabChip/FeedbackAuthorLine/Chip. Already codified in CLAUDE.md Strict Reusable UI Principle.
- Figma specs: user pastes exact SwiftUI snippets / provides SVGs; use screenshots + provided files, avoid get_design_context.

## Open / next
1. User to build/run and review card vs Figma (image #6 layout).
2. Offered but not done: composer raw TextField → AppInputField (chat-style; awaiting user). Overflow ⋮ + send ↑ icons still SF (not in exported frame) — offered to export.
3. Still-pending from prior: remove dead DebugSettingsView "Feedback: Admin Mode" row (long-press header is the real admin entry)?
4. Prior deviations un-confirmed: users see own unapproved posts in Open tab; composer up-arrow send.
5. Code review fixes (auth bypass, replies-to-replies, page clamp, slideInSheet) all applied earlier — see auto_2026-07-02_17-32.md.

## Key API refs
AppButton(title:action:hierarchy:size:leftIcon:iconColorOverride:) — memberwise init, skip defaulted params. AppButton.secondary = white bg + linePrimary border + shadow + purple icon (override with iconColorOverride). AppIcon template inherits foregroundColor. New files auto-included (PBXFileSystemSynchronizedRootGroup). SVG imageset needs Contents.json with preserves-vector-representation:true.
