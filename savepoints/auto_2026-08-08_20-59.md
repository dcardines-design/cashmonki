# Savepoint — CashMonki — 2026-08-08 20:59

Branch `firestore-source-of-truth`. HEAD `bdf63a3e` (docs: What's New copy for 3.7.0 (12)) —
committed this session. Everything below `bdf63a3e` is **UNCOMMITTED**.
Build-verified: `xcodebuild -project CashMonki.xcodeproj -scheme "Cashooya Playground"
-sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -configuration Debug build`
→ BUILD SUCCEEDED. SourceKit still throws bogus "Cannot find X in scope" — stale index, trust
xcodebuild only.

## 3.7.0 shipped
App Store Connect app `6755343516` ("Cashmonki: Budget & Expense"), project 272347 in PostHog.
iOS 3.7.0 build **12**, What's New already pasted. History: Prepare for Submission Aug 7 8:23 PM →
Waiting for Review 9:37 PM → In Review Aug 8 1:04 AM → **Ready for Distribution Aug 8 8:14 AM**.
Live. Verified the archive binary contains the placeholder strings, so build 12 does have it.

## Placeholder "not working" — resolved, no code change
`AskTypingPlaceholder` (AskPage.swift ~1330) was fine. The user's device had **Reduce Motion ON**;
`start()` bails to a single static `"Ask anything"`. Turning Reduce Motion off made typing + cycling
work. **Open decision:** whether to ship a Reduce Motion path that cross-fades through all 7 phrases
instead of freezing on one (user never answered yes/no). Also noted `Task { await run() }` at
AskPage.swift:1363 is NOT MainActor-isolated (SWIFT_VERSION = 5.0) — mutates @State off-main; worth
fixing with the same edit.

## Feedback board ghost items — FIXED (uncommitted)
`FeedbackManager.init` called `seedMockData()` synchronously, so 4 fake rows
("I want this feature", authors `dcardines03@example.com` / `carlsdungeon_cralwer@example.com`)
rendered until the Firestore listener's first snapshot replaced `items` ~1s later. Not gated by
`#if DEBUG`, so it shipped to the App Store. `seedMockData()` deleted entirely, call site removed,
two stale doc comments corrected. Board is now Firestore-only; empty collection = empty board.

## Admin / feedback board access (answered, no change)
`UserManager.swift:146` — `adminEmails = ["dcardinesiii@gmail.com"]`, the only allowlisted admin.
`FeedbackManager.isAdmin` gates the New tab / Approve / Change Status. The `isAdminMode` debug
toggle and the SettingsPage `" (admin)"` suffix + 3-tap role trick are both behind
`#if DEBUG || targetEnvironment(simulator)`, so on release there is NO visible admin indicator —
only signal is whether the board shows 3 tabs. Ground truth is the `role` field on the Firestore
user doc. Gmail aliases (`+4`) are NOT admin.

## Ask analytics — BUILT (uncommitted)
Audit first: 51 `AnalyticsEvent` cases, 34 wired, **17 never fired** (see task #1). Ask had only 2
calls total (AskChatService.swift:733 budget_created, :833 subscription_created); Feedback board has
zero.

10 new events added to `AnalyticsEvent`, all fired via `AnalyticsManager.shared.track` so PostHog +
Mixpanel both receive them. No message text / amounts / merchants — shape, counts, timing only.
`ask_message_sent` (source typed|starter_chip, character_count, word_count, turn_index),
`ask_reply_received` (reply_type, latency_ms), `ask_reply_failed`, `ask_reply_unrenderable`
(envelope_type, raw_length), `ask_limit_reached`, `ask_card_shown`, `ask_card_confirmed` (+via),
`ask_card_cancelled`, `ask_card_superseded` (card_type, replaced_by), `ask_batch_rows_added`
(rows_added, rows_total, rows_done, added_all_at_once).

Supporting additions: `AskDisplayMessage.Content.analyticsName` and
`AskStructuredMessage.analyticsName` (stable snake_case names); `AskChatManager.SendSource` enum +
`send(_:source:)`; `turnStartedAt` / `elapsedTurnMilliseconds()` for latency;
`trackCardResolved(_:confirmed:extra:)` called next to all 18 `confirmedDrafts.insert` sites
(cancel* funcs pass `confirmed: false`).

## TestFlight vs App Store split — BUILT (uncommitted)
User's TestFlight purchases were polluting the live dashboard. Added
`PostHogManager.buildEnvironment` → `"debug"` / `"testflight"` (receipt filename `sandboxReceipt`) /
`"app_store"`, plus `isProductionInstall`. `registerBuildEnvironment()` registers super properties
`app_environment` + `is_production_install`; called right after `PostHogSDK.shared.setup(config)` in
`CashMonkiApp.swift` (registering in `init` would land before setup). Stub added to the
non-PostHog `#else` class. Also removed the stray `PostHogSDK.shared.capture("test_event")` that
fired every launch.
**Caveat told to user:** only tags events from the next build on — filtering
`app_environment = app_store` hides all historical data too.

## Files touched (uncommitted)
`CashMonki/Functions/FeedbackManager.swift`, `CashMonki/Functions/PostHogManager.swift`,
`CashMonki/CashMonkiApp.swift`, `CashMonki/Pages/AskPage.swift`,
`CashMonki/Models/AskMessageSchema.swift`

## Next steps
1. **Commit.** Suggested split: (a) kill feedback mock seed, (b) Ask analytics events,
   (c) app_environment super property + test_event removal.
2. Runtime-test the new events in the simulator, confirm they land in PostHog project 272347 with
   `app_environment=debug`.
3. Dashboard work (task #3) — https://us.posthog.com/project/272347/dashboard/941658. Add Ask funnel
   (message_sent → card_shown → card_confirmed), abandonment by card_type, latency trend,
   unrenderable count, limit hits. Set the `app_environment = app_store` filter. ASC tab is open and
   the user is signed in; PostHog needs its own login (user must sign in — I don't authenticate).
4. Task #1 — wire the 17 dead events: app_became_active, app_resigned_active, onboarding_skipped,
   transaction_viewed, roast_message_attached, wallet_edited, wallet_switched, budget_edited,
   recurring_subscription_paused, recurring_subscription_resumed, currency_changed,
   currency_conversion_used, subscription_cancelled, trial_started, settings_opened,
   language_changed, sync_failed.
5. Decide the Reduce Motion placeholder fix (see above).
6. Still open from earlier savepoints: `UserManager.updateBudget` Firebase sync commented out (AI
   budget EDITS are local-only); recurring/subscription Ask cards never runtime-tested; Sign in with
   Apple unverified on a physical device.

## User prefs
Caveman FULL. Data PRECIOUS. AskUserQuestion for choices. UI changes need approval (requested =
approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose. No subagents or
workflows unless asked.
