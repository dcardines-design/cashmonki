# CashMonki Savepoint — 2026-07-02 17:32

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI "CashMonki"). iOS 18 target, local git only, Xcode open. No CLI build → user builds with ⌘R. **UI changes need user approval.** Figma MCP connected (Rosebud Studio team; file key `ranVMXtnIZLnq6W5FVXxt6`). Verify Swift edits with `xcrun swiftc -parse <file>` (SourceKit "cannot find AppColors/UIKit" inline diagnostics are whole-module/macOS-SDK noise, NOT real errors).

## This session (continues from auto_2026-07-02_17-11.md)

### 3. Status Chip components (Figma) — DONE
Pulled Figma nodes 1614-7277 (Done) + 1614-7318 (In progress); sampled exact colors via pure-Python PNG decode (no PIL/ImageMagick on machine).
- `Components/Chip.swift` — NEW reusable chip: HStack spacing 4, padding h8/v6, cornerRadius 4, optional SF icon + text, caller colors.
- `DesignSystem/AppColors.swift` — added statusDoneForeground #00A73D / statusDoneBackground #ECFEF6 / statusInProgressForeground #E17100 / statusInProgressBackground #FFF4C6 / statusPendingBackground #F2F5F7.
- `Models/FeedbackModels.swift` — FeedbackStatus now uses those colors + "wrench.fill" icon for In progress.
- `Components/FeedbackComponents.swift` — StatusPill reimplemented via Chip (cornerRadius 4, was Capsule).

### Feedback inbox tabs — matched chart-filter TabChip treatment
`Sheets/FeedbackInboxSheet.swift` tabs now HStack(spacing 8) + padding(6) + backgroundWhite + radius 16 (same as Expense/Income/Balance filter).

### Code review (/code-review high, wf_4cb01124-4b6) — 10 findings, FIXES APPLIED
- #1 🔴 comment-delete auth bypass → FIXED: FeedbackCommentActionSheet takes `canDelete`, Delete disabled/greyed when !canModify; onDelete guards canModify.
- #2 🔴 replies-to-replies silently lost → FIXED: FeedbackManager.addReply resolves parent top-level comment (target OR comment containing the reply), flattens to one level.
- #3 🔴 admin mode unreachable (debugSection not rendered anywhere) → FIXED: long-press "Feedback Inbox" header (0.8s) toggles isAdminMode + haptic. (Note: DebugSettingsView "Feedback: Admin Mode" row still exists but is UNREACHABLE dead code — user asked whether to remove it; AWAITING answer.)
- #5 🟠 carousel page index not clamped on shrink → FIXED: .onChange(of: pages.count) clamps currentSubscriptionPage. (HomePageComponents)
- #7 🟡 dead delete-post branches → FIXED: actionSheet(for:) is comment-only now.
- #8 conventions → FIXED: feedback full-screen sheets use .slideInSheet (SettingsPage + inbox); tab bg AppColors.backgroundWhite not literal .white.
- Left intentionally: #4 carousel cap (user chose UNBOUNDED), #6 email-prefix identity collision (only matters with real auth; mock now), #9 raw TextField composer + #10 raw Buttons (specialized chat/icon controls; codebase uses raw Button for icon buttons e.g. SheetHeader), inbox grey root bg (design intent), feedbackRelativeTime vs TransactionSyncManager.formatRelativeTime DRY.

## Open / next steps
1. ANSWER PENDING: remove the dead DebugSettingsView "Feedback: Admin Mode" row? (long-press is now the real entry)
2. User to build/run and review visuals (chips, carousel, feedback board). Admin view: Settings → Feedback → long-press title.
3. Earlier deviations still un-confirmed: users see own unapproved posts in Open tab; composer uses up-arrow send button.
4. Later: swap FeedbackManager mock → Firestore when auth re-enabled (then fix #6 identity properly).

## Key API refs
Sheets: SheetHeader.basic, FixedBottomGroup.primary, AppButton(title:action:hierarchy:size:leftIcon:) / .secondary(_:size:leftIcon:){}, AppInputField.text/.amount, TabChip.basic. Bottom sheets: .sheet + .presentationDetents([.height(x)]). slideInSheet(isPresented: Binding) is now just a fullScreenCover wrapper. New files auto-included (PBXFileSystemSynchronizedRootGroup). Figma screenshot flow: get_screenshot returns short-lived URL → curl to scratchpad → Read.
