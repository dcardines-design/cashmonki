# Savepoint — CashMonki — 2026-08-02 02:07

## State
`/Users/DGC/Desktop/cashooya-playground`. Build:
`xcodebuild -project CashMonki.xcodeproj -scheme "Cashooya Playground" -destination 'id=A67ED167-504D-4122-A118-475C3A37B63F' -configuration Debug CODE_SIGNING_ALLOWED=NO build`
SourceKit cross-file errors = FALSE POSITIVES. Version **3.6.0 (4)**. Branch `firestore-source-of-truth`, clean, pushed. HEAD `b9bbc1dd`.

## ✅ LIVE SYNC IS WORKING — verified on device
Simulator run confirmed, first end-to-end proof this session:
```
📡 CloudSync: attached 4 listeners for Zalijpio
📡 CloudSync: applied transactions — 694 local + 697 cloud → 697
```
Three transactions arrived from the other device with no relaunch. Also verified: **no AppCheck 403s** (debug token worked), `🎯 Budgets: 1` survived launch (budget-wipe fix confirmed), new transaction saved and echoed back through the listener with no write loop.

## Firebase console work done this session (via Chrome)
- **App Check debug token registered** as `sim-A67ED167` = `11D5313F-2FC6-4244-98ED-C6EDCE576988` (regenerates on simulator wipe).
- **Firestore rules hardened and published** (live "Today 1:55 AM"). The DEPLOYED rules had `allow read, write: if true` on `/users/{userId}` and all subcollections — every user's financial data was publicly readable/writable. Now `request.auth != null && request.auth.uid == userId`. Repo `firestore.rules` updated to match (`18e4e278`).
- **Verified in console**: transaction `BCFCDC41` did NOT exist server-side despite the app logging "Successfully saved" — Firestore acks writes locally before the server accepts. Uploads had been silently failing under App Check.

## Fixed this turn
`UserManager.adoptMissingWallets()`, called from `applyCloudTransactions`. Log showed 698 transactions but only 695 counted: 3 synced rows referenced wallet `86BE3DCA`, absent from this account's wallet list, so every view filtered them out. Now adopts a placeholder wallet (`.distantPast` stamp) so the rows stay visible under their OWN wallet; when the real wallet syncs, `mergeAccounts` overwrites the stub by id. **User explicitly chose merge over reassignment** — reassigning would silently move money between wallets.

## Open items from the same log (user was asked, chose the wallet fix first)
1. **Misleading connection test** — `Listen for query at _connection_test failed: Missing or insufficient permissions.` Now BY DESIGN: `_connection_test` is top-level and the new rules close everything outside `users/<uid>` and `feedback_items`. But it prints `❌ Firebase connection test FAILED` every launch. Point it at `users/<uid>` instead.
2. **`Modifying state during view update`** — dozens per merge; every `applyCloudTransactions` cascades `AccountManager.filteredTransactions` inside SwiftUI's update pass. Undefined behavior; defer the apply off the update pass.
3. Transactions listener re-reads all ~698 docs on attach — wants incremental `whereField("syncMetadata.lastModified", isGreaterThan:)`.

## Still open (unchanged)
- **Category gaps A + C**: uid-scope `UnifiedCategories`/`CategoryHierarchy` (RISKY — `CategoriesManager.init()` loads synchronously before auth resolves; copy `SubscriptionManager.swift:55,182-192`; safety snapshot; guest categories MERGE into account per user decision), then carry categories in `connectLocalBox`. `CurrencyPreferences` has the same global-key bug — batch with A.
- **Write consolidation (stage 2)**: one `CloudSync.push` entry point replacing ~52 call sites across 9 files.
- Small: "Pull Data from Firebase" silently no-ops when sync is off (`SettingsPage.swift:2249`); subtitle "Data is only stored locally on this device" inaccurate.
- **TestFlight**: archive in Xcode (Shift+Cmd+K first). Signing fine (Automatic, team 8Q3Z8WY5D9). Clean: flag defaults false, no hardcoded key, `ITSAppUsesNonExemptEncryption=false`, debug paths unreachable. Still needed: RevenueCat "Transfer to new App User ID", screenshots, reviewer demo account, privacy-policy AI clause + URL placeholder in `APPSTORE_LAUNCH_3.6.0.md`, Sign in with Apple check.
- `OPENROUTER_API_KEY not found` → receipt scanning fails on this build.
- RevenueCat `Test` offering references non-existent product ids; real `Cashmonki` offering fine.

## Technique notes
- `print()` never reaches the unified log. Read it via `xcrun simctl terminate … && xcrun simctl launch --console-pty … > /tmp/log 2>&1` (backgrounded; `timeout` isn't installed).
- Xcode 16 puts app code in `CashMonki.debug.dylib`, not the thin `CashMonki` binary — `strings` on the executable falsely looks stale.
- Firebase console rules editor is CodeMirror: `document.querySelector('.CodeMirror').CodeMirror.setValue(...)` works via javascript_tool; typing full rules would fight auto-indent.
- Figma: whole-icon-group exports come back polluted with page background; use the individual `svgAssets` entry or a 4x PNG.

## User prefs
Caveman FULL. Data PRECIOUS. **Use the AskUserQuestion choices UI whenever input is needed.** Figma: load `skill://figma/figma-design-to-code/SKILL.md`, then `get_design_context` + `download_assets`. UI changes need approval (requested = approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose.
