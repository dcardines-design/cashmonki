# CashMonki Savepoint — 2026-07-05 19:19 (streaming/persistence, stat cards, TestFlight LIVE)

Project: `~/Desktop/cashooya-playground/`. ⌘R = RELEASE config. Verify: `xcrun swiftc -parse` (SourceKit noise IGNORE). Continues auto_2026-07-05_02-49.md. **CAVEMAN MODE ACTIVE**.

## SHIPPED TO TESTFLIGHT — v3.5.0 (build 2 pending re-archive)
3.4.0 train closed → bumped 3.5.0 (plist CFBundleShortVersionString + 6× MARKETING_VERSION); build # → 2 (CFBundleVersion + 6× CURRENT_PROJECT_VERSION) after post-upload changes. ITSAppUsesNonExemptEncryption=NO added (skips encryption Qs). dSYM FBAEMKit warning = ignorable. What's-New copy given. **App confirmed running on user's phone via TestFlight; chat works with real data.** Install-over keeps data (same bundle id+team). "Build only device" dialog = destination still "Any iOS Device" → pick sim/phone.

## Ask chat — EIGHT types now: +stat (Figma 1643-7904)
1. **stat**: caps title 10 kern1 grey / value Overused Grotesk MEDIUM 50 (user spec; was bold48) w/ ₱ symbol / trend row arrow.down.right|up.right 13 bold + "12% vs June" 15 grey; pads 18/16, spacing 8, r16 (reduced from 24), border. Trend color SEMANTIC: good_direction ("down" default) → teal improvement else red (mock showed red-down; flagged, user didn't object). Value SOFT-VALIDATED: must ≤ cached-rows total ×1.05+1 else "couldn't verify" text; % computed client-side from value/compare_value. Prompt type 8.
2. **compare_spending tool**: periods[{label,from,to}] ≤12 + optional category → rows {label,amount,count} → charts (bar per period) & stats. Fixes "can't chart comparisons" (user hit live).
3. **STREAMING (SSE)**: streamCompletion reads bytes.lines "data:" chunks; text deltas forward to UI only when decided-plain (first non-ws char ≠ '{'/'`' else buffer silently); tool_calls assembled from indexed fragments (id/name/args concat); onStreamReset withdraws streamed text if tool round follows; rawAssistant rebuilt for loop. Manager: streamingMessageID bubble grows per delta; finish() settles/cleans (em-dash scrub) or swaps structured; error removes bubble.
4. **PERSISTENCE**: AskDisplayMessage+Content Codable (synthesized; id param init; content now var); Documents/ask_chat_v1.json {messages ≤100, confirmed}; persist() on every append/confirm/cancel (utility queue, atomic); load in init (welcome only if empty). AskChartItem Codable + items in CodingKeys (SAFE: service always overwrites items/rows on live decode).
5. **AskThinkingDots**: 3× 7pt circles cycling opacity/scale 0.3s timer.
6. **Scroll-down button** (Figma 1643-8006): 40pt white circle arrow.down, border+shadow, straddles composer top (offset -20, overlay .top on bottom group), shows when distance-from-bottom >240 (onScrollGeometryChange), tap → eased bottomAnchor. ScrollViewReader HOISTED above ZStack.
7. **Charts**: values ₱-symbol (AskFormat.currencyWithSymbol, primary symbol; also table currency cells); "limit": N field (chart+table) for top-N (rows pre-ranked); card border 4px #F3F5F8 inset 2; **palette = 20-step sequential brand-purple ramp** (hue 0.70, sat .95→.20, bright .45→.98, dark=biggest; barTextColor white first half / ink second). Palette history: vivid 6 → muted 8 (validated) → user chose purple ramp.
8. **Tools full-history**: get_recent_transactions +category/merchant/from/to filters, cap 200 (was 25, user hit gap live); get_spending_by_category periods week/month/quarter/year/all + from/to; get_budgets (limit/spent/remaining per period).
9. Composer: send clears FIRST (autocorrect race) + restores if rejected mid-thinking; send icon 50% when empty OR thinking; Return sends+clears+dismisses (empty → just dismiss); tap-away + scroll dismiss keyboard. Draft tile ₱ symbol.
10. transaction_delete + convert_currency + context-trim 20 + tap-cards→ReceiptDetailSheet (chat.detailTransaction sheet) — earlier this session.

## Feedback board
- Firestore live: seeds now COMMENT-FREE → then seed-upload REMOVED entirely (remote = truth; empty = empty state; mock only if no Firebase). Collection wiped 2× (`npx firebase-tools firestore:delete feedback_items -r --force`). makeSampleThread deleted; seed upvoters example.com (3-4).
- Post composer REVERTED to original grey-pill TextField (kept send-01 icon) per user.
- Role tap-trick on profile name (3× → admin, 1× → user, sim/debug) + UserManager.setRole persists; ensureRoleAssigned fills nil only (allowlist forces).
- firestore.rules still NOT deployed (console check first). Task #9 in_progress.

## Misc
Subscriptions carousel: LazyHStack(alignment: .top) — partial pages hug top-left. Ask navbar icon = template tint same as others (confirmed). Chat charts count: ONE (bars). Stat variants 2 (side-by-side w/ vertical mini bars) + 3 (budget progress) designed in mock — NOT built; user's Image #52 shows variant 2 design.

## Next
1. User re-archive → TestFlight build 2 (streaming/persistence/stat/scroll-btn all post-build-1).
2. Offered: comparison side-by-side stat cards (variant 2), budget progress stat, message caps for free users (#13), key server-side before App Store.
3. #9 rules deploy after console check. #5 email notify (no go). #6 billing (user blocked).
