# CashMonki Savepoint — 2026-07-05 01:52 (Ask chat: polish + update/single-transaction types)

Project: `~/Desktop/cashooya-playground/`. User runs ⌘R (RELEASE config — sim gates use `#if DEBUG || targetEnvironment(simulator)`). Verify: `xcrun swiftc -parse` (SourceKit noise = IGNORE). Continues auto_2026-07-04_23-42.md. **CAVEMAN MODE ACTIVE** ("normal mode" stops).

## Ask chat — CONFIRMED WORKING end-to-end (add flow + tables tested in sim by user)
Message menu now FIVE types (AskMessageSchema.swift): text / table / transaction_draft / **transaction_update** / **transaction** (1-3 cards). All strict-validated, fence-stripping, text fallback.

### This session's additions
1. **transaction_update**: model must use exact ids from get_recent_transactions rows (rows now include "id"; prompt forbids showing id column). UI = Figma permission card: grey r16 container, `UnifiedTransactionDisplay.row` on white r12 inset 8, bottom "Change ₱X to ₱Y?" + purple "Yes" (.textPrimary). NO Cancel per design (cancelUpdate exists unused). Confirmed state: same card, row shows updated values live + "Transaction updated" + teal check (successForeground); no extra chat message. AskChatService.updateTransaction rebuilds via CurrencyRateManager (sign/conversion) preserving txID/createdAt/receipt/recurring → UserManager.updateTransaction. Flagged: mock's strikethrough-old-value-inline not done (stock component can't).
2. **transaction (single/multi cards)**: {"type":"transaction","transaction_ids":[..]} 1-3 → stacked UnifiedTransactionDisplay.row white cards r16 border, 8 gap; schema hard-caps 3; prompt: 4+ MUST be table.
3. **Draft tile redesigned** (user mock): caps EXPENSE/INCOME label (from selected category type, live), title semibold16 "☕️ Add 100 under coffee?" (emoji=TxnCategoryIcon.emojiFor, target=category lowercased), meta "Jul 4, 2026 · Starbucks" 15 medium, VStack spacing 12; container: HStack spacing20, pad L20/R30/V20, r16 border; plus = AppButton secondary 2xs = INSTANT CONFIRM (tile body tap = expand editor).
4. **Tables**: fit-vs-scroll heuristic (est. width 7.5pt/char vs screen-40 → full-width equal cols 2-line wrap ELSE h-scroll natural widths 1-line + 10pt col gaps); numeric-column auto right-align; currency format cells 14pt; scroll variant clipped to message width w/ scroll-aware 20pt white fades (onScrollGeometryChange, hidden at rest ends).
5. **Composer**: one line (plus/mic HIDDEN; bare send-01 icon purple 22pt); card pad h20/bottom10 (top removed), r16, borderless, subtle shadow (0.06 opacity r16 y12); floats ZStack bottom over thread w/ 240pt bottom-fading ultraThinMaterial blur (mask gradient); thread bottom pad 180.
6. **Starter chips** (fresh thread only, above composer, h-scroll): AppButton .secondary 3xs "📊 Show my recent transactions" / "💸 What did I spend this month?" / "➕ Add 120 for lunch at Jollibee"; tap sends (emoji stripped).
7. **Anim**: message fade-in 0.18s; scroll-to-bottom on every append (deferred 1 runloop, easeInOut 0.35, anchor bottom); chips 0.15s.
8. **User bubble**: pad 20/14, Color(0.96,0.96,0.97), r12. Msg spacing 28.
9. **OpenRouter key**: in Info.plist OpenRouterAPIKey (verified live, HTTP 200); migrates to keychain on launch.
10. Em-dash scrub (.text replies) + prompt rule.
11. Got-feedback card final pads: L18/R20/T12/B16.

## Answered / decided
- Supabase shift from Firebase: recommended YES staged (Edge Fn unblocks receipts w/o Google billing; RLS solves #9; auth migration = community tool or re-login). AWAITING user answers: real user count? org slot (Rosebud full: bambooks active, suki paused)?
- Chat persistence: NONE (in-memory singleton; dies on relaunch). Offered Codable→Documents JSON + cap question. Not built.
- Claude-style table handling explained → adopted clipped-container variant + re-added fades.

## Task list: #5 email notify (no go), #6 billing (user blocked), #7 user rebuild/verify feedback backlog, #8 composer swap + dead debug row, #9 Firestore-or-Supabase migration (Supabase leaning).

## Next candidates
1. User test: update flow ("change my last lunch to 150"), single cards, 4+ table rule.
2. Chat persistence build. 3. transaction_delete type (same pattern). 4. Supabase decision → schema. 5. Strikethrough custom row variant if user insists.
