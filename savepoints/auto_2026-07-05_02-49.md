# CashMonki Savepoint — 2026-07-05 02:49 (chat delete/chart/budgets + Firestore feedback migration)

Project: `~/Desktop/cashooya-playground/`. ⌘R = RELEASE config (sim gates: `#if DEBUG || targetEnvironment(simulator)`). Verify: `xcrun swiftc -parse` (SourceKit noise IGNORE). Continues auto_2026-07-05_01-52.md. **CAVEMAN MODE ACTIVE**.

## Ask chat — now SEVEN types: text/table/transaction_draft/transaction_update/transaction/transaction_delete/chart
1. **transaction_delete**: 1-10 ids; permission card (rows + "Delete this/these N?" + RED Delete via textColorOverride destructiveForeground); confirmDelete → UserManager.removeTransaction; "🗑️ Deleted N." chat msg.
2. **Multi-updates**: transaction_update accepts transaction_ids (≤10, same change); card stacks rows, pluralized question, "N transactions updated" confirmed state (check icon = our "check" asset teal, label 15pt).
3. **chart** (Figma 1641-7816): white card r16 border, title 10pt medium kern1 UPPERCASE grey (user spec), rows: label 104pt grey 15 / bar h28 r8 value inside white 12pt pad10 / {n} count right; pitch 40; bars scale to max (min 64), maxBarWidth = screen-40-36-104-12-30-12. **Palette VALIDATED via dataviz skill** (all 6 checks pass): #542EFF purple, #0B8A6E teal, #E8590C orange, #D6336C pink, #1971C2 blue, #8D6708 ochre — fixed order, never reshuffle. Items injected client-side from cached tool rows (label=category/merchant/label, value=amount/value, count). get_spending_by_category rows now include "count".
4. **Tools added**: get_budgets (category/period/limit/spent-computed-per-period/remaining; quarterly≈month start), convert_currency (CurrencyRateManager.convertAmount; prompt: never estimate rates).
5. **Decoder: embedded-JSON extraction** — prose+JSON mixed replies ("Here they are: {…}") now parsed via balanced-brace scan (string-aware); fixed live bug user hit.
6. **Context trim**: history = last 20 messages.
7. **Tap targets**: single-card rows → chat.detailTransaction → ReceiptDetailSheet (.sheet(item:) on AskPage, update/delete wired).
8. **Scroll FIX**: bottomAnchor marker AFTER 180pt clearance + defaultScrollAnchor(.bottom) + scroll on isThinking; onAppear lands at bottom (tab open).
9. Composer: container top pad removed (card v-pad 10 restored), r16, borderless, shadow only, NO fill (blur-only bg, hugs container, fade top 35%); bare send-01 icon (no button); plus/mic hidden; chips ABOVE input (AppButton .secondary 3xs, h-scroll, 12pt gap to input, 4th chip "📈 Chart my spending this month").
10. Msg spacing 28; fade anims 0.15-0.18s; user bubble 20/14 Color(.96,.96,.97) r12.

## Feedback board → FIRESTORE (task #9 IN PROGRESS)
- **Models**: FeedbackItem/Comment `upvoters: [String]` (emails) replaces stored upvotes/upvotedByMe (now computed: count / contains(currentUserEmail)). Seeds use 3 fake example.com upvoters.
- **FeedbackManager**: `feedback_items` collection; snapshot listener (startSync, 1s-delayed from init, isSyncing guard); ALL mutations saveRemote (setData whole doc, comments embedded) / deleteRemote; encode/decode via JSONEncoder .secondsSince1970 → JSONSerialization. Firebase absent → mock; remote empty → seeds upload once. Listener sorts createdAt desc (NOTE: overrides inbox upvote-sort? No — inbox filteredItems still re-sorts ✓).
- **firestore.rules WRITTEN, NOT DEPLOYED** (+ firebase.json "firestore" entry): feedback read=auth'd, create=authorEmail==token.email, delete=author-only, update=any auth'd (admin needs custom claim = blocked on billing). users/transactions matches = permissive auth'd placeholder. **DO NOT blind-deploy: replaces unknown console rules; user must check Console→Firestore→Rules first.** Listener needs signed-in Firebase Auth user; permission-denied → console rules block collection.
- **Role tap trick**: SettingsPage profile name tap 3× → "(1)","(2)","(admin)" sets role admin via UserManager.setRole (persists local+Firebase); 1 tap while admin → user. Sim/debug gated. ensureRoleAssigned no longer clobbers manual role (allowlist forces admin; else only fills nil).

## Tasks
#7 rebuild/verify: DONE (user). #9 Firestore: client done; REMAINING = verify console rules → deploy, test listener auth, custom-claim admin later. #5 email notify (no go). #6 billing (user blocked). Supabase alternative discussed earlier — user chose Firestore by naming it.

## Next
1. User rebuild → feedback board: watch "📮 FeedbackManager" logs (listener errors = rules/auth issue).
2. Rules deploy after console check (`npx firebase-tools deploy --only firestore:rules` from repo root).
3. Chat: streaming + persistence still offered; budget-chart mapping ("spent" key) offered.
4. Caveman skills need CC restart to appear as /caveman commands.
