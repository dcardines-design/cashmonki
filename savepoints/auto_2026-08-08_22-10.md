# Savepoint — CashMonki — 2026-08-08 22:10 — shipping 3.7.1 (13)

Continues `auto_2026-08-08_21-27.md`.

## State
Branch `firestore-source-of-truth`, HEAD `fc58d846`, **tree clean**. Six commits today:
- `bdf63a3e` docs: What's New copy for 3.7.0 (12)
- `50ad4ff1` fix: fake feedback posts flashed on the board before the first snapshot
- `efa9106d` feat: analytics for the whole Ask flow
- `96d905c6` feat: tag every PostHog event with the build environment
- `20c8bb98` feat: fire the analytics events that were declared but never sent
- `32e956f7` feat: analytics for the feedback board
- `fc58d846` chore: 3.7.1 (13)

`MARKETING_VERSION = 3.7.1` and `CURRENT_PROJECT_VERSION = 13` (6 occurrences each in
project.pbxproj). Info.plist uses `$(CURRENT_PROJECT_VERSION)`, nothing hardcoded.
BUILD SUCCEEDED on `-sdk iphonesimulator -destination 'generic/platform=iOS Simulator'`.

## Analytics: 64 declared, 64 wired, 0 dead
Three cases deliberately removed rather than wired: `currency_conversion_used` (fires per row
render — volume, not signal), `app_became_active` / `app_resigned_active` (duplicate PostHog's
built-in Application Opened / Backgrounded).

Verified live in PostHog project 272347 from the simulator, all tagged `app_environment=debug`:
`ask_message_sent`, `ask_reply_received`, `ask_card_confirmed`, `feedback_submitted`,
`feedback_upvoted`.

Still never fired: `ask_card_shown`, `ask_card_cancelled`, `ask_card_superseded`,
`ask_batch_rows_added`, `ask_limit_reached`, `ask_reply_failed`, `ask_reply_unrenderable`,
`feedback_commented`, `feedback_deleted`, `feedback_approved`, `feedback_status_changed`.

## Dashboard 941658 — 8 new tiles, created via the PostHog REST API
API worked when the browser UI kept timing out. `POST /api/projects/272347/insights/` with
`dashboards: [941658]` in the body puts a tile straight on the dashboard.
Insight ids: 10859273 Ask messages sent, 10859290 Ask funnel, 10859291 Ask reply latency,
10859295 Ask card outcomes by type, 10859296 Ask errors and limits, 10859339 Feedback submitted,
10859340 Feedback engagement, 10859341 Feedback moderation.

No `app_environment` filter on them — all Ask data so far is `debug`, so filtering to `app_store`
would show empty charts. **Open decision:** apply `app_environment = app_store` to the
purchase/subscription tiles once 3.7.1 is live. That is the original TestFlight-pollution complaint.

## API key — REVOKE
Personal API key `cashmonki-dashboard-cli` (`phx_…jXQE`, scopes insight:write + dashboard:write)
stored at `~/.posthog_key`, mode 600. **Its value was pasted into the chat transcript in
plaintext.** The user said they would revoke it. When they confirm, delete `~/.posthog_key`.
It lacks `query:read`, so reading events back has to happen in the browser.

## Real production traffic observed
`jangatchalian0327@gmail.com` was actively using the App Store build during this session —
transactions, paywall views, StoreKit review prompt. Their events interleave with simulator events
in the same feed, which is exactly what `app_environment` is meant to separate. Old shipped builds
still emit `test_event`; that stops only once 3.7.1 is out.

## Simulator
Booted `A67ED167-504D-4122-A118-475C3A37B63F` (iPhone 16 Pro Max), bundle `com.dante.cashmonki`,
current build installed. **Always `xcrun simctl terminate` before `simctl install`** — installing
over the running app truncated Firestore's LevelDB WAL earlier and the app SIGABRT'd on launch
(`FirestoreClient::Initialize` assertion). Recovery was deleting only
`<container>/Library/Application Support/firestore`.

## Next step, in progress: submit 3.7.1 for review
1. Archive: Xcode → Product → Archive (scheme **"Cashooya Playground"**, Any iOS Device).
2. Upload: Organizer → Distribute App → App Store Connect. **This machine has no ASC API key**
   (`~/.appstoreconnect/private_keys` is empty), so the upload cannot be scripted — it needs the
   Xcode GUI, or a key would have to be created first.
3. In ASC (app `6755343516`): create the 3.7.1 version, paste `WHATSNEW_3.7.1.md`, attach build 13,
   submit for review. The ASC browser tab is open and signed in; I can drive this part.
4. After release: apply the `app_environment = app_store` filter to purchase tiles.

## Carried over
- Reduce Motion placeholder: still shows one static phrase; proposed cross-fade through all 7 plus
  `@MainActor` on the cycling Task (`AskPage.swift:1363`, project is Swift 5 mode so `@State` is
  mutated off-main). Never approved.
- `UserManager.updateBudget` Firebase sync still commented out — AI budget edits are local-only.
- Recurring/subscription Ask cards never runtime-tested.
- Sign in with Apple unverified on a physical device.
- Firestore has no recovery path for a corrupt local store; `clearPersistence()` behind a
  repeat-crash flag would be the hardening move. Low priority.

## User prefs
Caveman FULL. Data PRECIOUS. AskUserQuestion for choices. UI changes need approval. Never `sed -i`.
Secrets never committed or exposed. No subagents/workflows unless asked. I do not sign into
accounts or enter credentials on the user's behalf.
