# CashMonki Savepoint — 2026-07-02 18:23 (user closing session; restart planned for local Figma MCP)

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI "CashMonki"). iOS 18, local git only, Xcode open, no CLI build → user runs ⌘R. **UI changes need user approval. "Use existing components always instead of creating from scratch."** Verify edits: `xcrun swiftc -parse <file>` (inline SourceKit "cannot find AppColors/UIKit/FeedbackItem" = isolated-parse/macOS-SDK noise — IGNORE).

## Figma MCP
- Cloud `claude_ai_Figma` works (screenshots/metadata/SVG export). File key `ranVMXtnIZLnq6W5FVXxt6`.
- Just added **figma-desktop** (http://127.0.0.1:3845/mcp, user scope) → available AFTER restart; `figma-dev-mode` pending approval (user runs `claude` to approve). Local server reads live selection (no node-id needed).
- Flow: get_screenshot/download_assets → curl short-lived URL into scratchpad → Read. download_assets(svg) embeds whole frame — extract inner `<g id="...">` paths.
- User prefers pasting exact SwiftUI specs; avoid get_design_context.

## Feature state (all parse clean; NOT yet visually verified by user in this round)
### Recurring/Subscriptions carousel — DONE (earlier)
Paged 2×2, dots, unbounded (user choice), page clamp onChange. HomePageComponents/HomePage/Utils/Array+Chunked.

### Feedback board — near-final, sessions ago→now:
- Sheets: FeedbackInboxSheet (tabs Open/Done [+New admin], TabChip strip padding(6)/bg backgroundWhite/radius16, top pad 20), FeedbackPostSheet, AddFeedbackSheet, ChangeStatusSheet, FeedbackCommentActionSheet. All full-screen via .slideInSheet, EXCEPT post detail opens via **.fullScreenCover(item: $selectedItem)** — fixed blank-page race (don't revert to isPresented+separate ID).
- FeedbackManager: mock store, isAdminMode (UserDefaults `feedbackAdminModePreview`), long-press inbox header 0.8s toggles admin (+haptic). All 3 approved seed posts auto-populated via makeSampleThread() (amanda09 comment + dcardines03 creator reply); 4th unapproved, no comments.
- FeedbackCard: chip top-left, upvote top-right; title semibold16 + desc **overusedGroteskRegular(14)** (NEW helper; registered OverusedGrotesk-Roman.ttf in Cashooya-Playground-Info.plist UIAppFonts — needs rebuild to load); container pad h20/v18, radius12, inset(0.5) line1stLine border.
- FeedbackAuthorLine: spacing 8, 2×2 dot separators fill line1stLine, optional commentCount. CreatorBadge = successForeground green.
- Chip: pad h8/v5(-2px), radius4, iconAsset+iconFallback via AppIcon. Status colors: Done #00A73D/#ECFEF6, InProg #E17100/#FFF4C6, Pending fg secondary/#F2F5F7 (AppColors.status* tokens).
- UpvoteButton.boxed = AppButton .secondary **.doubleExtraSmall** leftIcon "thumbs-up" iconColorOverride purple-when-upvoted (count text stays black — AppButton limitation, user informed). thumbs-up.svg = user's 20×20 OUTLINE untitledui (purple outline when upvoted; offer filled variant if wanted).
- Icons in Assets.xcassets imagesets: thumbs-up, message-square-01, tool-01, check, **dots-vertical** (new, exported from node 1614-10765); clock reused from DesignSystem assets.
- Post detail (aligned to Figma 1614-6842 "to the T"): root VStack spacing 24, padding h24/top22/bottom24, action row spacing 20 = upvote + Comment + **post ⋮** (dots-vertical). Post ⋮ → FeedbackCommentActionSheet post branch (actionTargetComment=nil): canEdit false, Reply→startNewComment, Delete for owner/admin (deleteFeedback + dismiss). Comment ⋮ also dots-vertical now.
- Settings entry: "💡 Feedback" row in Support section.

## Code-review (wf_4cb01124-4b6) — fixed: delete auth bypass, reply-to-reply lost, admin unreachable (long-press), page clamp, dead branches, slideInSheet + colors. Left intentionally: unbounded carousel, email-prefix identity (mock only), raw TextField composer + icon Buttons (consistent w/ codebase), inbox grey root bg.

## Open threads / next steps
1. User to REBUILD & visually verify: post detail vs Figma #13, card container border, regular-weight desc (font needs clean rebuild), dots-vertical, smaller upvote.
2. UX flaws identified (user asked; fixes NOT yet requested/applied): #1 approval-gate invisible ("Pending" collides w/ moderation state), #2 no sort-by-upvotes, #3 no submit toast, #4 can't edit own post, #5 reply-to-reply loses @mention. High-value trio = 1/2/3 — offered.
3. Dead code: DebugSettingsView "Feedback: Admin Mode" row unreachable (debugSection never rendered) — remove? (asked, unanswered)
4. Composer TextField → AppInputField? (offered, unanswered). Send ↑ icon still SF.
5. Later: Firestore swap when auth returns (+ real identity fixes #6).

## API cheatsheet
AppButton(title:action:hierarchy:size:leftIcon:iconColorOverride:); sizes: medium/small/extraSmall/doubleExtraSmall/tripleExtraSmall; .secondary = white+line1stLine border+shadow, icon purple (overridable), title color NOT overridable. AppIcon(assetName:fallbackSystemName:size:) template-recolors. AppFonts.overusedGroteskRegular/Medium/SemiBold/Bold(size:). Imageset = folder + Contents.json preserves-vector-representation. New files auto-included (synced folders). Model switched to Fable 5 for next session.
