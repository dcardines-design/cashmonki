# CashMonki Savepoint — 2026-07-02 18:55 (feedback board Figma alignment round)

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI "CashMonki"). iOS 18, local git only, Xcode open, no CLI build → user runs ⌘R. **UI changes need user approval; use existing components always.** Verify edits: `xcrun swiftc -parse <file>` (SourceKit "cannot find AppColors/FeedbackItem" inline diagnostics = isolated-parse noise — IGNORE). Continues from auto_2026-07-02_18-23.md.

## Figma MCP — WORKING
**figma-desktop (local, http://127.0.0.1:3845/mcp) now works after restart** — reads live selection, no node-id needed. Used get_metadata + get_screenshot on live selection this session. User drives by selecting in Figma + pasting CleanShot measurement screenshots. File key ranVMXtnIZLnq6W5FVXxt6; post design node 1614:6842.

## This session's changes (all parse-clean, NOT yet rebuilt/verified by user)
1. **Filled thumbs-up on upvote**: new asset `Assets.xcassets/thumbs-up-filled.imageset` (outline path filled+stroked, 20×20). FeedbackComponents.swift UpvoteButton: both .boxed and .inline swap asset when isUpvoted (SF fallback hand.thumbsup.fill).
2. **Comment button = AppButton**: FeedbackPostSheet action row Comment is now AppButton .secondary (was raw Button w/ stroke), leftIcon message-square-01, iconColorOverride foregroundPrimary.
3. **Outline comment icon**: `message-square-01.imageset` SVG REPLACED — was the SOLID variant; now same path stroked (viewBox 12, stroke-width 1.2 ≈ stroke 2 @20px). Affects all usages.
4. **Smaller post action buttons**: upvote + Comment → `.tripleExtraSmall` (16px text). UpvoteButton gained `boxedSize: ButtonSize = .doubleExtraSmall` param — inbox FeedbackCard unchanged.
5. **Comment section per measured specs**: comments now in white radius-16 tile on grey `AppColors.surfacePrimary` scroll bg, tile inset 12, inner pad h24/v22, 28 gap between comments AND comment→replies; replies block: 1px line1stLine thread line, indent 12+line+15 spacing (total 28 as before). Tile hidden when no comments. Divider after action row removed. Post section keeps white bg pad h24/top22/bottom24. Text→icons gap in comment row = 12 (spacing 6 + .padding(.top,6) on action HStack).
6. **Comment action sheet rebuilt**: FeedbackCommentActionSheet = 3× AppButton .secondary .doubleExtraSmall, VStack spacing 16, .padding(20), no SheetHeader, top-aligned; detent 280→230 in FeedbackPostSheet. dismissThen (0.05s delay) kept.
7. **AppButton: new `textColorOverride: Color?`** (after iconColorOverride; disabled→foregroundTertiary) — used for red Delete. NOTE param order in memberwise init: ...isEnabled, iconColorOverride, textColorOverride. AppButton does NOT read .disabled() env — pass `isEnabled:` param.
8. **Behavior change flagged to user**: Delete in action sheet now ALWAYS red when enabled (was red only for admin-moderating-others). User may want old distinction back.

## Gotchas learned
- Mid-edit duplication bug happened in FeedbackComponents (reply Button duplicated) — fixed; watch Edit old_string scope.
- AppButton icon resolution: customIconNames set OR UIImage(named:) — SVG assets resolve via UIImage at runtime, fine.
- Overscroll at top of post sheet now shows grey (scroll bg surfacePrimary) — user may ask to pin white at top.

## Next steps
1. User REBUILD (⌘R) & visually verify everything above + previous round (regular-weight card desc font, dots-vertical, post detail vs Figma).
2. Pending from last savepoint: UX trio (approval-gate visibility, sort-by-upvotes, submit toast) offered not requested; dead DebugSettingsView admin row removal unanswered; composer TextField→AppInputField unanswered; Firestore swap later.
3. Possibly align remaining feedback screens (inbox card?) to Figma via live selection flow.
