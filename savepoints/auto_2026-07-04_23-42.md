# CashMonki Savepoint — 2026-07-04 23:42 (AI chat "Ask" tab shipped + feedback polish)

Project: `~/Desktop/cashooya-playground/`. User runs ⌘R. Verify: `xcrun swiftc -parse` (SourceKit inline noise = IGNORE). Continues auto_2026-07-04_18-00.md. **CAVEMAN MODE ACTIVE** (full; "normal mode" to stop). NOTE: user's ⌘R builds are RELEASE config (Xcode won't reload my scheme Debug edit) → sim/debug gates use `#if DEBUG || targetEnvironment(simulator)`.

## AI CHAT ("Ask" tab) — WORKING END-TO-END
Design: Figma 1635-6605 (chat), 1639-7891 (add-tile), 1639-7821 (composer). User's structured-message spec implemented in full. NO MCP server exists in repo (user prompt claimed one; flagged) — "tools" = local layer over managers.
- **Models/AskMessageSchema.swift**: menu {text, table, transaction_draft}; strict Codable validation; markdown-fence stripping (model wrapped JSON in ```json → was rendering raw; FIXED); any failure → plain text fallback. New types = schema + renderer.
- **Functions/AskChatService.swift**: OpenRouter direct, model `anthropic/claude-sonnet-4.5`, key via Config keychain path. Tool loop (max 4): get_recent_transactions / get_spending_by_category (UserManager data) / get_categories (CategoriesManager, real UUIDs). Tables: model sends columns/title ONLY; rows injected client-side from cachedRows[tool_call_id] ?? lastRows → can't invent numbers. `createTransaction(...)` NOT in model tool menu; mirrors AddTransactionSheet (CurrencyRateManager.shared.createTransaction sign/conversion → UserManager.addTransaction). System prompt: 3 formats, ask-don't-guess on missing amount, real category ids only, NO EM DASHES (+ client-side "—"→"," scrub in AskChatManager, welcome msg reworded).
- **Pages/AskPage.swift**: AskChatManager (@MainActor singleton; confirmedDrafts/draftErrors); thread (user grey bubbles r12, assistant plain text 16 medium); AskTableMessageView (schema-driven, currency cols right-aligned + smart format via AskFormat.currency); draft = collapsed add-tile ("➕ Add X for Y?" + purple plus, Figma 1639-7891) → expands to confirm card: AppInputField amount/merchant/date/note, category chips (top suggestion pre-selected, validated vs findCategoryOrSubcategoryById; "Other…" → CategoryPickerSheet **ID-based init** selectedCategoryId:), Confirm→write+✅ msg, Cancel→collapse, invalid amount→inline error draft stays. **Composer FLOATS**: ZStack(alignment:.bottom) over thread, scroll bottom pad 180; card = VStack spacing 24 pad 20, white, r24, shadow (0.06,0.09,0.16)@0.14 r32 y32, line1stLine border, outer pad 24; buttons = AppButton .tripleExtraSmall .fixedSize (secondary plus + microphone-01 w/ iconColorOverride black; primary send-01).
- **Assets NEW**: stars-01 (navbar), send-01, microphone-01 imagesets (from ~/Downloads, preserves-vector).
- **Navbar**: Tab enum + case .ask; order Home, Transactions, Budgets, **Ask**, Profile; AskPage in ContentView switch.
- **OpenRouter key**: user pasted into Cashooya-Playground-Info.plist `OpenRouterAPIKey` (line ~52); verified live curl HTTP 200; migrates to keychain via Config.initializeAPIKeys() on launch.
- **Verified in sim**: model called tools, returned real category UUIDs (Drinks/Dining) for "Add 120 coffee" — only fence bug blocked render (now fixed, NOT yet re-tested).

## Feedback board polish this session
Approve button: leftIcon "check" asset (was SF "checkmark"), size .doubleExtraSmall. See more → .textPrimary (purple). StatusPill: full opacity, .allowsHitTesting(feedback.isAdmin) (was .disabled=dimmed). ChangeStatusSheet: ScrollView removed (3 rows), pad bottom 20, detent 420→382. Admin toggle REMOVED from inbox header (user request; isAdminMode UserDefaults may be stuck ON — offer reset if user complains; email-role admin unaffected). Got-feedback card: pad top 12 / bottom 16 / h 20 (Figma-matched).

## Task list
Done #1-4. Pending: #5 email notify (Vercel+Resend, still no explicit go), #6 billing (USER blocked adding billing account; steps given; then curl app-config), #7 USER rebuild+verify backlog, #8 feedback composer → AppInputField.multiline + remove dead DebugSettingsView row, #9 Firestore migration (rules/custom claim/replyToUserID; also chat could move server-side then).

## Next steps
1. User re-test chat: "Add 120 coffee" → should render tile+card now; test table ("show my recent transactions"); test Confirm → check Transactions tab + toast.
2. Chat polish candidates: streaming, persistence (chat resets on relaunch), attachment/mic stubs, suggestion-tile type from old design if wanted.
3. Em-dash scrub only on .text replies (not table titles) — extend if user notices.
