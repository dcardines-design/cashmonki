# Savepoint — CashMonki — 2026-08-02 00:55

## State
`/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Working build command:
`xcodebuild -project CashMonki.xcodeproj -scheme "Cashooya Playground" -destination 'id=A67ED167-504D-4122-A118-475C3A37B63F' -configuration Debug CODE_SIGNING_ALLOWED=NO build`
SourceKit "Cannot find 'AppColors'/'UserManager'" errors = FALSE POSITIVES, ignore.
Version 3.6.0 (3); App Store live 3.5.2.

**Git: clean tree, everything pushed.** Branch `firestore-source-of-truth` on origin (created this session; `main` untouched at `67499630`). 7 commits ahead:
`78c56554` turn-off-backup confirmation + opt-out key fix · `638c35b8` category sync LWW · `c927963f` docs/savepoints · `a82a2b1d` Data settings group · `4574096c` connect-device-data sheet · `cb63bb20` sync data bar carousel · `ab40b255` crisis fixes + duplicate collapse

## Shipped this session (all build-verified, NONE runtime-verified)
- **Sync data bar** promo carousel (Figma 1710-7185); text layout matched to feedbackPromoCard (16/12, 4pt gap, 18/20/12/16 padding) so carousel pages line up.
- **Duplicate subscription fix**: `UserManager.collapseGeneratedDuplicates` in `mergeTransactions`, keyed (subscriptionId ?? recurringTemplateId, due minute, amount), deterministic winner, loser tombstoned.
- **Connect-device-data sheet** rebuilt (Figma 1711-7570 tile / 1713-7674 empty state): disk asset 56×58, 20pt name, 14pt meta, "LAST UPDATE …" 9pt/0.9 tracking via `relativeAge`, hard shadow drawn as an offset RoundedRectangle (`.shadow(radius:0)` renders a halo — do NOT revert), empty state vertically centred.
- **Settings "Data" group** between Preferences and Feature Usage.
- **Category cloud sync** steps 0–2: seeds stamped `.distantPast`, record-level LWW on `updatedAt` honouring `isDeleted`, push guard refusing a pristine factory set, push after a successful fetch that found nothing new, hierarchy merge REMOVED (union resurrects deleted subcategories).
- **Turn-off-backup confirmation sheet** (Figma 1714-8158) + `AppButton.destructive` + `FixedBottomGroup` stacked actions/`destructiveThenPrimary` + both new sheets now use shared `SheetHeader`.
- **Opt-out key bug fixed**: was keyed on `currentUser.id` (fresh `UUID()` every sign-in, `AuthenticationManager.swift:108`), so "backup off" was silently re-enabled on next login by `migrateEnableSyncIfNeeded`. Now `syncUID()`, legacy key read once and adopted.

## Decisions made by the user this session
- Red token: **keep #de4706**, dropped the Figma #ef4444 (`destructiveRed` deleted). Kept `destructiveSurface = destructiveForeground.opacity(0.09)` for the callout tint.
- `SheetHeader`: **keep 18pt/20h/16v** — new sheets diverge from Figma's 20pt/20 on purpose.
- Footer: **extend FixedBottomGroup** (done) rather than keep a local footer. Note it pads bottom 34 vs Figma's 20.
- Savepoints + APPSTORE doc: committed to the repo.
- Next priority: the sync opt-out bug (done).

## Open items
**Category work (scoped, agreed order B→D→A→C; B+D done):**
- **A** — uid-scope `UnifiedCategories`/`CategoryHierarchy` keys. RISKY: `CategoriesManager.init()` loads synchronously at singleton creation, before auth resolves, so it needs a deferred/reload-on-auth path or it writes pristine defaults under a garbage key. Copy `SubscriptionManager.swift:55,182-192` (storageKey + `_legacy_backup` + delete global). Add a safety snapshot. User already decided: a guest's categories should MERGE into the account they log into.
- **C** — carry categories in `connectLocalBox` (needs A).
- `CurrencyPreferences` (`userPrimaryCurrency`/`userSecondaryCurrency`) has the same global-key bug; batch with A. Leave `DailyUsageManager` counters global on purpose (per-uid = free-tier bypass).

**Small fixes not done:**
- "Pull Data from Firebase" silently no-ops when sync is off (`SettingsPage.swift:2249`) — needs a toast.
- Row subtitle "Data is only stored locally on this device" is inaccurate; suggested "Not backing up — this phone only".

**Verification debt — nothing this session has run on a device.** Highest value: two-device category test (rename on A → appears on B), reinstall test (real categories come back, not defaults), and watching for `🧹 UserManager: collapsed duplicate occurrence` on launch.

**Launch blockers (3.6.0):** real-device TestFlight pass; possible build bump to (4); RevenueCat "Transfer to new App User ID"; Firestore rules published; App Check debug token for A67ED167; screenshots; reviewer demo account; privacy-policy AI/OpenRouter clause + the URL placeholder in `APPSTORE_LAUNCH_3.6.0.md`; confirm Sign in with Apple if any social login is offered.

## User prefs
Caveman FULL. Data PRECIOUS. Figma: load `skill://figma/figma-design-to-code/SKILL.md`, then `get_design_context` + `download_assets` — real assets, never redraw. Node exports of a whole icon group come back polluted (page background rect); grab the individual `svgAssets` entry or a 4x PNG instead. UI changes need approval (requested = approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose.
