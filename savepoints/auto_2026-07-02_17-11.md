# CashMonki Savepoint — 2026-07-02 17:11

Project: `~/Desktop/cashooya-playground/` (iOS/SwiftUI app "CashMonki", target internally "Cashooya Playground"). iOS 18 target, local git only. Xcode is open. **UI changes require user approval before implementing** (per CLAUDE.md).

## Session work (2 features)

### 1. Recurring/Subscriptions carousel — DONE
Turned the stacked 2-col grid into a horizontally **paged 2×2 carousel** with page dots + edge peek.
- `Pages/HomePageComponents.swift` — `recurringTransactionsSection` rewritten: `subscriptions.chunked(into: 4)` → paged `ScrollView(.horizontal)` using `containerRelativeFrame` + `.contentMargins(.horizontal,20)` + `.scrollTargetBehavior(.paging)` + `.scrollPosition(id:)`, bled out of parent padding via `.padding(.horizontal,-20)`. Dots hide + scroll disabled when ≤1 page. Added `subscriptionPageGrid(_:)` helper.
- `Pages/HomePage.swift` — added `@State currentSubscriptionPage: Int? = 0`.
- `Utils/Array+Chunked.swift` — new reusable `chunked(into:)`.

### 2. Feedback board — BUILT, awaiting build/visual review
Feature-request board. Decisions locked: **local mock data first** (swap Firestore later, auth is disabled) · **one role: creator = app team/admin** · **user side + hidden admin toggle** · **admin sets all statuses**. Entry = Settings row. Real author names = email prefix.

Status model: `approved: Bool` (false → admin "New" tab only) + `status: Pending/InProgress/Done`. Tabs: user Open/Done, admin New/Open/Done.

New files:
- `Models/FeedbackModels.swift` — FeedbackItem, FeedbackComment (1-level replies), FeedbackStatus, FeedbackTab.
- `Functions/FeedbackManager.swift` — ObservableObject singleton, mock seed matching mockup, all mutations, `isAdminMode` (persisted UserDefaults key `feedbackAdminModePreview`), `feedbackRelativeTime()`.
- `Components/FeedbackComponents.swift` — FeedbackAvatar, CreatorBadge, StatusPill, UpvoteButton(boxed/inline), FeedbackAuthorLine, FeedbackCard, FeedbackCommentRow (@mention highlight).
- `Sheets/FeedbackInboxSheet.swift`, `FeedbackPostSheet.swift`, `AddFeedbackSheet.swift`, `ChangeStatusSheet.swift`, `FeedbackCommentActionSheet.swift`.

Wiring:
- `Pages/SettingsPage.swift` — `@State showingFeedback`, "💡 Feedback" row in Support section, `.fullScreenCover` for FeedbackInboxSheet.
- `Pages/DebugSettingsView.swift` — `@ObservedObject feedbackManager`, `debugFeedbackRows` "🛡️ Feedback: Admin Mode" toggle.

## Verification
All new + edited files pass `swiftc -parse` (clean). Inline SourceKit "cannot find AppColors/FeedbackItem" diagnostics are whole-module indexing noise (isolated parse), NOT real errors. No CLI build configured — user builds in Xcode (⌘R).

## Next steps
1. User builds/runs; review feedback board visuals vs mockup, iterate.
2. Two flagged deviations to confirm: (a) users see their own unapproved posts in Open tab (vs strict hide); (b) composer uses up-arrow send button (vs return-only).
3. Test admin flow: 7-tap version → debug → toggle admin mode → reopen Feedback.
4. Later: swap FeedbackManager mock for Firestore once auth re-enabled.

## Key API notes (this codebase)
- Sheets: `SheetHeader.basic`, `FixedBottomGroup.primary`, `AppButton(title:action:hierarchy:size:leftIcon:)` / `.secondary(_:size:leftIcon:){}`, `AppInputField.text/.amount`, `TabChip.basic`. Bottom sheets: `.sheet` + `.presentationDetents([.height(x)])`.
- Colors `AppColors.*` (accentBackground=purple #542eff, successForeground green, accentOrange, destructiveForeground, surfacePrimary, linePrimary). Fonts `AppFonts.overusedGroteskMedium/SemiBold/Bold(size:)`.
- New files auto-included (project uses PBXFileSystemSynchronizedRootGroup).
