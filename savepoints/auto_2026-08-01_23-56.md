# Savepoint — CashMonki — 2026-08-01 23:56

## State
`/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Build command that works:
`xcodebuild -project CashMonki.xcodeproj -scheme "Cashooya Playground" -destination 'id=A67ED167-504D-4122-A118-475C3A37B63F' -configuration Debug CODE_SIGNING_ALLOWED=NO build` → **BUILD SUCCEEDED** (last run this session). SourceKit "Cannot find type/'AppColors'/'UserManager'" errors = FALSE POSITIVES, ignore.
Version 3.6.0(3); App Store live 3.5.2. Branch `firestore-source-of-truth` @ a7a1db5 — **still nothing committed this session**.

## ⚠️ TEMPORARY DEBUG FLAG — REVERT BEFORE SHIPPING
`HomePageComponents.swift` `promoCarousel`: `let forceSyncPromoPreview = true` inside `#if DEBUG`. Forces the sync nudge page to show while signed in so the carousel can be seen without logging out. Set false (or delete the block) before archiving.

## Done this session (all building, none user-verified on device)
1. **Sync data bar** — `syncPromoCard` rebuilt from Figma 1710-7185 as one 400×67 vector (`sync-data-bar.imageset`), aspect-locked, white title / white-85% subtitle. Old `sync-cloud`/`sync-receipt` imagesets deleted.
2. **Duplicate subscription txn fix** — `UserManager.collapseGeneratedDuplicates` called at end of `mergeTransactions`. Key = (`subscriptionId ?? recurringTemplateId`, due minute, amount); winner = newer `lastModified`, tie → smaller uuid; loser tombstoned. Root cause: `SubscriptionManager` (~:383) generates with no existence check, so each device mints its own UUID for the same due date.
3. **Connect-device-data sheet** (`DataBoxPickerSheet.swift`) rebuilt from Figma, several revisions:
   - container 1711-7253: no top padding, 20 sides/bottom, gap 24
   - tile 1711-7570 (final): horizontal 28 / vertical 20, gap 20, radius 12, 1pt #dce2f4 border, **hard shadow drawn as an offset RoundedRectangle** (`.shadow(radius:0)` caused a halo above the card — do not go back to it), `strokeBorder` not `stroke`
   - disk `data-box-disk.imageset` re-exported from node 1711-7573 (226×234 @4x), rendered 56×58
   - name 20pt SemiBold, meta 14pt #72788a gap 6 with 3pt Circle dot, text stack spacing **8**
   - last line 1711-7638: "LAST UPDATE 1W AGO" — 9pt Medium, `.tracking(0.9)`, uppercase, secondary; bound to `box.updatedAt` via new `relativeAge(_:)` helper
   - chevron 24pt, `foregroundSecondary`
   - **empty state** 1713-7674: `data-box-empty.imageset` (268×280 @4x) at 67×70, "No local data yet" 22pt SemiBold + shared `introCopy` 18pt, gaps 20/6, padding 20 sides / 24 vertical
   - DEBUG sample tiles were added then **removed at user request** — no sample code remains.
4. **Settings regroup** — new `dataSection` (header "Data") between Preferences and Feature Usage, holding the sync row (guest CTA / `firebaseSyncToggleRow`) + "Connect device data"; both removed from `preferencesSection`.
5. **App Store pack** — `APPSTORE_LAUNCH_3.6.0.md` (repo root), char counts script-verified.

## Category backup — SCOPED, NOT IMPLEMENTED (user asked to scope first; awaiting go-ahead)
Categories DO already sync: `pushCategoriesToCloud()` from `saveCategories()` → `users/<uid>/categories/all`; `restoreCategoriesFromCloud()` on the `UserManagerFirebaseLoadComplete` notification (`Categories.swift:209`). Four gaps found:
- **A. Global keys** — `categoriesKey = "UnifiedCategories"` (:866), `hierarchyKey = "CategoryHierarchy"` (:174), plus legacy `CustomCategories`/`OverriddenBuiltInNames`/`CategoryReplacements`. No uid suffix → all accounts on one phone share one set. Precedent to copy: `SubscriptionManager.swift:55` `storageKey()` + `:182-192` legacy migration (adopt global → per-uid, keep `_legacy_backup`, delete global).
- **B. Merge is add-only** (`Categories.swift:944-956`, `where byId[c.id] == nil`) → renames/deletes/emoji edits never propagate between devices. Model already has `updatedAt` + `isDeleted` (:72-74), merge ignores both. Deletion is a soft delete (:1677).
- **C. `connectLocalBox`** (`UserManager.swift:1889-1907`) moves transactions/wallets/subscriptions but not categories. Blocked on A.
- **D. Push only fires on category edit** → a user who never edits categories has no cloud blob; fresh install shows "No Category". Needs a push after login/full sync.
Also global, decide separately: `CurrencyPreferences` (`userPrimaryCurrency` etc.) leaks across accounts. Deliberately leave `DailyUsageManager` counters global (per-uid = free-tier bypass). Onboarding flags stay local by design.
**Recommended order: B → D → A → C** (B is the correctness bug with no migration risk; A is the dangerous one, needs safety-snapshot care).
**Open question for user:** guest with custom categories logs into an existing account — merge their categories in, or leave them with the guest box? My recommendation: merge.

## Uncommitted groups (4 now)
1. 3.6.0 crisis fixes (CashMonkiApp, UserManager onboarding guard, HomePage)
2. sync data bar (HomePageComponents + `sync-data-bar.imageset`)
3. duplicate-occurrence fix (UserManager)
4. connect-device-data sheet redesign + settings "Data" group (`DataBoxPickerSheet.swift`, `SettingsPage.swift`, `data-box-disk`/`data-box-empty` imagesets)
Plus untracked `APPSTORE_LAUNCH_3.6.0.md` + savepoints. Committing as 4 separate commits was offered, not yet approved.

## Next steps
1. User: view the carousel with the DEBUG flag on, then tell me to revert it.
2. Decide the guest-category merge question; then implement B → D → A → C.
3. Commit the four groups.
4. Launch blockers unchanged: real-device TestFlight pass, possible build-number bump to (4), RevenueCat "Transfer to new App User ID", Firestore rules, App Check token, screenshots, reviewer demo account, privacy-policy AI clause, Sign in with Apple check.

## User prefs
Caveman FULL. Data PRECIOUS. Figma: use the MCP (`get_design_context` after loading `skill://figma/figma-design-to-code/SKILL.md`, plus `download_assets`) — real assets, never redraw with SF Symbols. UI changes need approval (requested = approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose.
