# Savepoint — CashMonki — 2026-07-20 12:14

## State
App `/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Build REQUIRES `CODE_SIGNING_ALLOWED=NO`. Version 3.6.0 / build 3. Active sim A67ED167. DerivedData `CashMonki-gefiakeveutzgnaaobjcdlxkkdxx`. SourceKit cross-file errors = FALSE POSITIVES.

## Git — branch `firestore-source-of-truth` @ a7a1db5
Prior commits: `5871cf4` sync cleanup + data-loss guardrails; `a7a1db5` Firestore Phase 1 (offline persistence + read layer, flag-off).
**UNCOMMITTED (today's work, ready to commit when card approved):**
- M CashMonki/CashMonkiApp.swift — gated Firestore persistence behind `FeatureFlags.firestoreSourceOfTruth` (fixes launch blank-screen hang); disabled recovery-dump leftovers (`dumpUserStorageForRecovery`/`restoreDanteData`, ~L51-52).
- M CashMonki/Functions/UserManager.swift — `updateOnboardingProgress(_:allowDowngrade:Bool=false)` MONOTONIC (fixes onboarding 16→2 downgrade); last-wallet-delete passes allowDowngrade:true.
- M CashMonki/Pages/HomePage.swift — `@State showingConnectAccount`, `@ObservedObject authManager`, `@State currentPromoPage`; `promoCarousel` replaces feedbackPromoCard (~L429); connect-account `.sheet` → LoginView(allowGuest:false).
- M CashMonki/Pages/HomePageComponents.swift — `promoCarousel` (paged, peek, dots like subscription cards; pages [.sync,.feedback] guests / [.feedback] logged-in), `syncPromoCard` (real Figma art + exact colors), `PromoPage` enum, `promoView(for:)`.
- ?? Assets: `sync-cloud.imageset` (240x78) + `sync-receipt.imageset` (279x191) — REAL art pulled via Figma MCP (node 1694-7193, file ranVMXtnIZLnq6W5FVXxt6).
Not pushed.

## Figma sync card — done, awaiting user visual confirm
Spec (exact): bg #87eaff, subtitle #01677c, Overused Grotesk SemiBold16/Medium12, radius10, pad top12/bottom16/px20, art=cloud+receipt+green refresh-ccw on right. Card shows for guests (Save data to cloud / Log in to sync), hides when logged in; tap → connect-account flow. Art offsets approximate (cloud x-2/y-10 w84; receipt x-16/y6 w104) — may need nudging.

## CRITICAL build gotcha (recurring)
DerivedData module cache corrupts → `module file 'Foundation-...pcm' not found` → builds SILENTLY FAIL → stale app runs, "changes don't apply." FIX: `rm -rf DerivedData/.../Build/Intermediates.noindex/ExplicitPrecompiledModules` + clean build. User must **Shift+Cmd+K then Cmd+R in Xcode** to see changes (their Xcode shares the cache; and Xcode Run overwrites my simctl installs).

## Verified fixes this session
- Blank-screen hang → fixed (persistence gated off by default).
- Onboarding restart on completed account → fixed (monotonic guard); box self-heals 2→16 (641 txns + name + default wallet). Read box ~10s after launch (App Check unregistered on A67ED167 → slow Firebase config).
- Good data intact: box ZalijpiofgQ, 641 txns, real categories, admin, onboarding 16.

## Next steps
1. User: Clean+Run in Xcode → view card; tune art offsets if needed.
2. Commit uncommitted work (crisis fixes + onboarding guard + Figma card/carousel).
3. Firestore migration Phase 2 (write path) — flag-gated. Plan: https://claude.ai/code/artifact/af206a68-195c-4614-aebc-fcf4f0923e02. Locked decisions: ask-chat single-thread per-msg docs; receipt imgs Firestore→Storage later; unify AccountData+SubAccount (preserve ids); security rules auth.uid==uid w/ Phase1; legacy stores reconcile→verify→delete-later.
4. User-side pending: RevenueCat transfer behavior → "Transfer to new App User ID"; Firestore security rules paste; register A67ED167 App Check debug token; re-archive 3.6.0(3) for TestFlight.

## User prefs
Caveman FULL. Data PRECIOUS. For Figma design tasks: USE the Figma MCP (get_design_context + download_assets) to pull real assets — don't recreate with SF Symbols. UI changes need approval (requested=approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose.
