# Savepoint — CashMonki — 2026-08-08 22:20 — 3.7.1 waiting on upload

Continues `auto_2026-08-08_22-10.md`, which has the full analytics detail. This one covers only
the submission state.

## Repo
Branch `firestore-source-of-truth`, HEAD `8333f5fb`, tree clean. 3.7.1 (13) in project.pbxproj.

## Done since last savepoint
- **ASC version 3.7.1 created** on app `6755343516`, status *Prepare for Submission*, metadata
  inherited from 3.7.0. What's New saved: "The feedback board no longer shows placeholder posts for
  a moment when you open it. It now waits for your real feedback to load."
- **Archive built**: `xcodebuild ... -sdk iphoneos -destination 'generic/platform=iOS'
  -configuration Release -archivePath ... archive` → ARCHIVE SUCCEEDED, 3.7.1 (13), 190 MB.
  Copied to `~/Library/Developer/Xcode/Archives/2026-08-08/CashMonki 3.7.1 (13).xcarchive` so it
  appears in Xcode Organizer.

## Blocked: binary upload needs the Xcode GUI
Two reasons the upload cannot be scripted from here:
1. No ASC API key (`~/.appstoreconnect/private_keys` empty) and no `AC_PASSWORD` keychain profile.
2. `security find-identity -v -p codesigning` shows **only** "Apple Development: Dante Cardines
   (Q2J92K333K)" — there is no Apple Distribution certificate, so `exportArchive` with an
   app-store method would fail. Organizer creates that cert during Distribute.

User's steps: Xcode → Window → Organizer → Archives → CashMonki 3.7.1 (13) → Distribute App →
App Store Connect → Upload → let Xcode manage signing. Processing takes ~5-15 min.

## Then I finish in the browser
ASC tab (tabId 864782305) is open and signed in on the 3.7.1 page.
1. Scroll to Build, attach build 13.
2. Click **Add for Review** (button is top-right of the 3.7.1 page).
3. Answer any export-compliance / content-rights prompts that appear.

## Still open
- **Revoke the PostHog key** `cashmonki-dashboard-cli` (value is in the chat transcript). Confirmed
  still Active as of 22:0x. When revoked, delete `~/.posthog_key`.
- Apply `app_environment = app_store` to the purchase/subscription tiles on dashboard 941658 once
  3.7.1 is live — that is the original TestFlight-pollution complaint.
- Untriggered events listed in the 22:10 savepoint (most Ask card events, most feedback events).
- Reduce Motion placeholder decision; `UserManager.updateBudget` Firebase sync still commented out;
  recurring/subscription Ask cards untested; Sign in with Apple unverified on device.

## Gotchas worth keeping
- Scheme is **"Cashooya Playground"**, not CashMonki. SourceKit errors in this project are a stale
  index — only xcodebuild counts.
- Always `xcrun simctl terminate` before `simctl install`, or Firestore's LevelDB WAL gets
  truncated and the app SIGABRTs at launch.
- PostHog's web UI kept timing out the Chrome extension; the REST API worked fine for creating
  insights (`POST /api/projects/272347/insights/` with `dashboards: [941658]`).
