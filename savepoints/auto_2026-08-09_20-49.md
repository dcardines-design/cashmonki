# Savepoint — CashMonki — 2026-08-09 20:49 — PostHog measurement rebuild

Continues `auto_2026-08-08_22-20.md` (3.7.1 submission state). This session was analytics, not shipping.
3.7.1 (13) upload status still unconfirmed — user never answered.

## Repo
Branch `firestore-source-of-truth`, HEAD `8333f5fb`. **Uncommitted** working-tree changes (see below).
Build verified: `xcodebuild -scheme "Cashooya Playground" -sdk iphonesimulator -destination
'id=C50E212F-BCE1-4953-92C0-8606A7EE94A0'` → **BUILD SUCCEEDED**.

## What was done

### 1. Audited the PostHog project (272347)
Found: 114 insights, 4 dashboards, 0 cohorts, 0 feature flags, 0 experiments. Retention and
Lifecycle insights existed but were the **default `$pageview` templates** — an iOS app sends no
pageviews, so they were empty. Only 2 funnels, one of them the dead pageview template.
**Zero of 114 insights filtered on `app_environment`** despite the property being registered
since 3.7.0 — the TestFlight-pollution complaint was never actually fixed.

### 2. New dashboard 1974766 — "CashMonki — Product Health (real users)", 13 tiles
https://us.posthog.com/project/272347/dashboard/1974766
Activation funnel · onboarding drop-off by step · paywall funnel ×2 (by `source`, by
`has_used_trial`) · receipt scan funnel + success rate · Ask confirm rate · Ask latency **p90/p95**
· weekly retention on `transaction_created` · lifecycle · weekly active transactors · error surface
· subscription guardrail. All verified rendering with real data.

### 3. Dedupe — 55 orphaned duplicates soft-deleted (114 → 59)
Rule: on **no** dashboard AND query-identical to one that **is** on a dashboard. Ids in
`<scratchpad>/deleted_insight_ids.json`. Restore with `PATCH /insights/<id>/ {"deleted": false}`.
The old "excl. specific person" variants hardcoded person id `019B5A1F-19AB-7648-8967-188BE69C9C15`
into each insight; the `is_internal` filter replaces that properly.

### 4. Legacy tiles filtered — 28 insights on dashboards 941658 / 988940 / 1018654 / 1000413
Verified still returning data after patch (Application opens 795, Transactions 333, 30d).

### 5. Attribution added in-app (uncommitted)
- `CashMonki/Functions/PostHogManager.swift` — `registerAttribution()`: `install_date` /
  `install_version` / `current_version` super props, plus Apple Search Ads via
  `AAAttribution.attributionToken()` → `https://api-adservices.apple.com/api/v1/` →
  `asa_campaignId`, `asa_adGroupId`, `asa_keywordId`, `asa_countryOrRegion`, `acquisition_source`.
  Cached in UserDefaults, 3 retries for Apple's post-install 404 window. `#if canImport(AdServices)`
  guarded, no-op stub added to the non-PostHog branch.
- `CashMonki/CashMonkiApp.swift:547` — call site, right after `registerBuildEnvironment()`.

## Key decision: the filter is NOT `app_environment = app_store`
Used `app_environment is_not ["debug","testflight"]` + `is_internal is_not_set`.
`app_environment` only exists on 3.7.0+ builds, so an exact match would silently discard every
older event. Verified data still flows after filtering (88 onboarding starts / 30d, not zero).

## Findings that matter more than the dashboards
- **Paywall: 78 real-user views → 0 trials → 0 subscriptions, 30d.** Unfiltered it's 171 → 2, so
  both conversions were TestFlight/internal. Events *do* fire in code
  (`RevenueCatManager.swift:181,190`; three paywall sheets). Unresolved whether this is a tracking
  bug on the real purchase path or genuine 0% conversion. **Verify before spending on marketing.**
- `source` breakdown: 76 `one_time_promo` vs 2 `app` — the promo intercepts nearly everyone before
  the normal paywall.
- Activation: 88 `onboarding_started` → 75 completed → **31 ever record a transaction** (35%).

## Corrections to earlier claims
- Session replay **is** enabled — `CashMonkiApp.swift:533`, `config.sessionReplay = true` with
  screenshot mode. Earlier "not enabled" came from a scope-denied API read.
- Ask latency **is** tracked (`latency_ms`, `AskPage.swift:388,479`) — but avg only, no percentiles
  until this session. Receipt scan still has no timing property.

## Blocked
`~/.posthog_key` is read/write for insights + dashboards but **missing `cohort:write`,
`query:read`, `event_definition:read`** — so no cohorts, no HogQL, no event volumes. Filters are
inlined per-insight as the workaround. Key is still Active and its value is in an old transcript;
user was told and chose to keep it.

## Next steps
1. Answer: fold attribution into 3.7.1 before upload, or ship as 3.7.2? Attribution collects
   nothing until a build with it is live.
2. Debug the 0% paywall conversion — watch replays of the 78 sessions; trace whether
   `subscription_started` fires on the real (non-sandbox) purchase path.
3. Reissue the PostHog key with the three missing scopes, then build real cohorts.
4. Marketing was the user's stated next topic. Assets ready but unused: 4 demo videos in
   `~/Documents` (`cashmonki ad 1`, `ad singapore`, sped PHP + USD demos), 243-line
   `CashMonki_AppStore_Optimization.md` never shipped to ASC, @dantecardines Twitter creds.
   Recommended order: fix conversion → ASC Custom Product Pages (free per-channel attribution)
   → ship ASO copy → organic short-form → only then paid.

## Gotchas
- Scheme is **"Cashooya Playground"**, not CashMonki. SourceKit errors here are a stale index
  ("No such module 'UIKit'", "Cannot find ContentView in scope") — only `xcodebuild` counts.
- PostHog insight refresh over 100 insights at once times out; refresh single ids with
  `?refresh=blocking`.
- Scratchpad scripts worth keeping: `ph_build.py`, `ph_dedupe.py`, `ph_filter.py` in
  `/private/tmp/claude-502/-Users-DGC/d47afb02-2875-4c3d-886d-74d60278a0db/scratchpad/`.
