# Savepoint — CashMonki — 2026-08-02 01:45

## State
`/Users/DGC/Desktop/cashooya-playground`, scheme "Cashooya Playground". Build:
`xcodebuild -project CashMonki.xcodeproj -scheme "Cashooya Playground" -destination 'id=A67ED167-504D-4122-A118-475C3A37B63F' -configuration Debug CODE_SIGNING_ALLOWED=NO build`
SourceKit "Cannot find 'AppColors'/'UserManager'" = FALSE POSITIVES.
**Version 3.6.0 (4)** — bumped for TestFlight. Branch `firestore-source-of-truth`, tree clean, all pushed (13 commits ahead of `origin/main`; `main` untouched at `67499630`).

## ⛔ BLOCKING: two Firebase console tasks (user-side, not code)
The simulator run proved the backend rejects this device — listeners cannot work until both are done:
1. **App Check debug token not registered** → `AppCheck failed: HTTP 403 "App attestation failed." PERMISSION_DENIED`. Token from the log: `11D5313F-2FC6-4244-98ED-C6EDCE576988` (regenerates on fresh install). Firebase Console → App Check → iOS app → Manage debug tokens.
2. **Firestore rules never published** → `Listen for query at _connection_test failed: Missing or insufficient permissions.` `firestore.rules` exists in the repo, undeployed.

**UNVERIFIED SUSPICION worth checking first:** the log shows `✅ Firebase: Successfully saved transaction BCFCDC41` *while* App Check was failing. The Firestore SDK acks writes locally before the server accepts them, so uploads may have been silently failing all along — which would explain two-device staleness better than the missing listener. Check the Firebase console for `users/ZalijpiofgQlumznBT0JcSmcms12/transactions/BCFCDC41`.

## Shipped since last savepoint (all build-verified; only the budget fix is runtime-confirmed)
- **Live cross-device sync** (`CloudSync.swift` + observers in `FirestoreService`): snapshot listeners on `users/<uid>`, its `transactions` subcollection, `categories/all`, `subscriptions/all`. Everything routes through existing merges (`mergeAccounts`, `mergeBudgets`, `mergeTransactions`, category LWW, subscription union) via new apply hooks: `UserManager.applyCloudUserDoc/applyCloudTransactions`, `CategoriesManager.applyCloudCategories(unified:pushIfNothingNew:)`, `SubscriptionManager.applyCloudSubscriptions`. Attach once per session, keyed on `syncUID()`, only after `UserManagerFirebaseLoadComplete`; detach on logout and when backup is toggled off.
- **CloudSync lazy-singleton bug FIXED** (`c5c67375`): nothing touched `CloudSync.shared` on launch, so its init never ran, the observer never registered, and no listener attached — 0 `📡` lines in a 1,354-line log. Now `CloudSync.shared.refresh()` is called from both startup branches in `CashMonkiApp.swift` (~:464 and ~:471).
- **Budget-wipe bug FIXED**: `loadFromFirebase` rebuilt `UserData` without `budgets:` (initializer defaults to `[]`, `Models.swift:1442`), blanking budgets on every cloud load. Now merged. **Runtime-confirmed**: budget added, saved, read back (`🎯 Budgets to save: 1`).
- **Turn-off-backup confirmation sheet** (Figma 1714-8158) + `AppButton.destructive` + `FixedBottomGroup` stacked actions/`destructiveThenPrimary`.
- **Sync opt-out key bug FIXED**: was keyed on `currentUser.id` (fresh `UUID()` per sign-in), so "backup off" was silently re-enabled next login. Now `syncUID()`, legacy key adopted once.
- **SheetHeader bumped to 20pt / padding(20)** — affects all 31 sheets. Turn-off sheet made full-height (detent dropped) to match AddTransactionSheet.
- Build bumped to (4).

## Decisions made by the user
- Red token: keep `#de4706`, dropped Figma's `#ef4444`. Kept `destructiveSurface = destructiveForeground.opacity(0.09)`.
- SheetHeader: bump to 20pt (chose this after first choosing "keep 18pt").
- Footer: extend `FixedBottomGroup` rather than keep a local one.
- Listeners: array-LWW accepted for budgets/subscriptions (my recommendation); attach once per session; stage 1 only — the 52 write call sites are NOT consolidated.
- TestFlight: bump to build (4); simulator smoke test before uploading.
- User asked that I use the AskUserQuestion choices UI whenever I need input.

## Other findings from the log (none blocking)
- `🛑 refusing onboarding DOWNGRADE 16 → 15` — our guard works, but `OnboardingStateManager` caps at 15 while `UserManager` stores 16, so it retries forever. Noisy, harmless.
- `⚠️ Environment: OPENROUTER_API_KEY not found` — receipt scanning will fail on this build. Ask chat works (goes through the backend).
- RevenueCat `Test` offering references non-existent `cashmonki_pro_yearly`/`cashmonki_pro_monthly`; the real `Cashmonki` offering is fine.
- Dozens of `Modifying state during view update, this will cause undefined behavior.` right after an Ask-created transaction. Real SwiftUI bug, unchased.
- Transactions listener re-reads all 694 docs on attach; wants an incremental `whereField("syncMetadata.lastModified", isGreaterThan:)` before the account grows.

## Still open
- **Category gaps A + C**: uid-scope `UnifiedCategories`/`CategoryHierarchy` (RISKY — `CategoriesManager.init()` loads synchronously before auth resolves; copy `SubscriptionManager.swift:55,182-192`; add safety snapshot; user decided guest categories MERGE into the account), then carry categories in `connectLocalBox`. `CurrencyPreferences` has the same global-key bug — batch with A. Leave `DailyUsageManager` global on purpose.
- **Write consolidation (stage 2)**: one `CloudSync.push` entry point replacing 52 scattered call sites across 9 files.
- Small: "Pull Data from Firebase" silently no-ops when sync is off (`SettingsPage.swift:2249`); subtitle "Data is only stored locally on this device" is inaccurate → "Not backing up — this phone only".
- **TestFlight**: archive must be done in Xcode (Shift+Cmd+K first — module cache corrupts and yields stale archives). Signing is fine (Automatic, team 8Q3Z8WY5D9). Verified clean: `firestoreSourceOfTruth` defaults false, no hardcoded API key, `ITSAppUsesNonExemptEncryption=false`, `debugSection`/`uploadSampleDataToFirebase` have no callers.
- Still needed for testers: RevenueCat "Transfer to new App User ID"; screenshots, reviewer demo account, privacy-policy AI clause + URL placeholder in `APPSTORE_LAUNCH_3.6.0.md`; Sign in with Apple check if any social login ships.

## Useful technique notes
- `print()` output does NOT reach the unified log. To read it without Xcode: `xcrun simctl terminate <udid> com.dante.cashmonki` then `xcrun simctl launch --console-pty <udid> com.dante.cashmonki > /tmp/log 2>&1` (run backgrounded; `timeout` is not installed on this Mac).
- Xcode 16 puts app code in `CashMonki.debug.dylib`, NOT the thin `CashMonki` binary — `strings` on the executable will wrongly look stale.
- Figma: whole-icon-group exports come back polluted with the page background; use the individual `svgAssets` entry or a 4x PNG.

## User prefs
Caveman FULL. Data PRECIOUS. Figma: load `skill://figma/figma-design-to-code/SKILL.md` then `get_design_context` + `download_assets`. UI changes need approval (requested = approved). Never `sed -i`. Secrets in CLAUDE.md/Info.plist — never commit/expose.
