# CashMonki Savepoint — 2026-07-07 15:40 (AI chat: subscriptions in spending + live markdown)

Project: `~/Desktop/cashooya-playground/`. ⌘R = RELEASE. Verify: `xcrun swiftc -parse` (SourceKit cross-file noise IGNORE). Continues auto_2026-07-06_22-30.md. **CAVEMAN MODE ACTIVE** ("normal mode" stops).

## Done this session (2 fixes)

### 1. Subscriptions missing from AI chat spending — `CashMonki/Functions/AskChatService.swift`
Root cause: subscriptions live in `SubscriptionManager.subscriptions` (separate `Subscription` model, persisted to UserDefaults key `cashmonki_subscriptions`), NOT in `currentUser.transactions`. Only `autoAddTransaction==true` subs get materialized as real Txns once due. `compare_spending` + `get_spending_by_category` read only `.transactions`, so tracked/non-auto-add subs (and due-but-not-yet-generated dates) were invisible → "monthly total lacking."
- New `subscriptionOccurrences(from:to:category:)`: iterates active EXPENSE subs, steps occurrences from `sub.createdAt` by `frequency.nextOccurrence`, capped at `now`, includes only dates in [from,to], SKIPS any day already covered by a real txn with matching `subscriptionId` (no double-count). `steps<2000` guard.
- New `subscriptionIsIncome(_:)` — mirrors SubscriptionManager's private `determineIsIncomeCategory` (via CategoriesManager); income subs skipped (spending = expenses only).
- Folded into `comparisonRows` (adds to total + count) and `spendingByCategoryRows` (mapValues tuple `(amount,count)`, fold occurrences in).
- NOTE: future periods show no subs (capped at now, matches real-txn semantics). Forward projection = separate feature if wanted.
- NOTE: raw `sub.amount` used (no currency convert) to match how auto-add materialized txns store amount. Pre-existing multi-currency inconsistency, out of scope.

### 2. `@MainActor` isolation error (Xcode: "Main actor-isolated property 'subscriptions' can not be referenced")
`SubscriptionManager` is `@MainActor`; `AskChatService` is plain `final class`, tool code non-isolated. (UserManager + CategoriesManager are plain ObservableObject → fine.)
Fix in `CashMonki/Managers/SubscriptionManager.swift`:
- `storageKey` → `nonisolated static let` (refs updated to `Self.storageKey`, 3 sites).
- Added `nonisolated static func persistedSubscriptions() -> [Subscription]` decoding the UserDefaults store (saved on every mutation → current; UserDefaults thread-safe).
- AskChatService now reads `SubscriptionManager.persistedSubscriptions()` instead of `.subscriptions`.

### 3. Markdown not rendering live during stream — `CashMonki/Pages/AskPage.swift`
Root cause: `AskStreamingWordsView` rendered each word as separate plain `Text` with `*`/`**` STRIPPED (per-word flow layout); bold/italic only appeared after settle (`Text(AskFormat.markdown(text))`). Cross-word markdown spans impossible in word-flow.
- Rewrote `AskStreamingWordsView` → single `Text(AskFormat.markdown(AskFormat.sanitizedStreaming(text)))`, 16pt medium, `.animation(value: text)`. Bold/italic now style live as words stream (text grows each 60ms tick).
- New `AskFormat.sanitizedStreaming(_:)`: hides a trailing DANGLING marker (odd count of `**`, then odd single `*` ignoring `**`) so `**partial` doesn't flash raw asterisks before closer arrives. Balanced markers render normally.
- Removed now-dead `AskFlowLayout` struct.
- TRADE-OFF: lost per-word opacity fade (incompatible w/ cross-word bold). Words still appear incrementally.

## Status
- NOT built/run yet. Isolated `swiftc -parse` clean on edited files. Red SourceKit diagnostics = cross-file/SDK noise (RevenueCatManager/AccountManager unresolved, `Subscription`→`any Subscription` misresolve). Rebuild to clear the 1 real actor issue (now fixed).
- Told user: running Debug build onto physical phone does NOT erase data (same bundle ID → container/UserDefaults/keychain kept; Firebase remote untouched). Only wiped if app deleted or bundle-ID mismatch. No model schema change this session → no local-store migration risk.

## Next
- VERIFY in sim/device: stream a `**bold**` reply, watch live styling + no raw-asterisk flash; run compare_spending / by-category with a non-auto-add subscription and confirm it now counts.
- Carry-over from prev savepoint: #5 email notify (Resend), #6 billing (blocked, receipts DOWN), new-chat button, retry/copy, receipt-in-chat, voice, category cleanup, line chart, budget stat, App Check enforcement, dead-code cleanup.
