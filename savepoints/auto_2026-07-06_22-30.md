# CashMonki Savepoint — 2026-07-06 22:30 (security ladder + chat polish, 3.5.1 archived)

Project: `~/Desktop/cashooya-playground/`. ⌘R = RELEASE config. Verify: `xcrun swiftc -parse` (SourceKit noise IGNORE). Continues auto_2026-07-05_19-19.md. **CAVEMAN MODE ACTIVE** ("normal mode" stops).

## SECURITY LADDER — done this session
1. **OpenRouter key OUT of binary**: Vercel proxy `https://cashmonki-proxy.vercel.app/api/chat` (edge, streams SSE; project dir ~/Desktop/cashmonki-proxy, env: OPENROUTER_API_KEY + APP_TOKEN; token also in .apptoken file). App: AskChatService baseURL→proxy, header `x-app-token: c0790e4a...a129` (revocable), missingAPIKey guard removed. Info.plist OpenRouterAPIKey EMPTIED (old installs keep stale keychain copy — unused). Verified: 200 w/ token, 401 w/o. NOTE: ~/.vercel in HOME links old "cookin-design" (untouched, no deploys).
2. **Anonymous Firebase Auth**: CashMonkiApp signs in anonymously post-configure (upgrades in place on real login). Console Anonymous provider ENABLED (user did steps A–D).
3. **Rules v3 DEPLOYED**: feedback create/update/delete require request.auth != null + shape checks (title≤200, detail≤2000, typed fields); read public; users/transactions still open (guest local-first sync); catch-all closed. Per-user ownership still client-side (needs real accounts; TODO in rules file).
4. **App Check**: code scaffolded (#if canImport(FirebaseAppCheck): AppAttestProvider device / DebugProvider sim, set BEFORE configure; factory class at CashMonkiApp bottom). User completed: FirebaseAppCheck product added to target, App Attest capability, entitlements appattest-environment=production (I set via plutil), console registration (Team ID 8Q3Z8WY5D9, TTL 1h). REMAINING (non-vital): sim debug token → console when convenient; Firestore ENFORCEMENT only after App-Check builds dominate.

## Chat additions this session
- **Rate limit**: DailyUsageManager +canSendChatMessage/recordChatMessage/getRemainingChatMessages/getChatUsageDisplayText (10/day free keys dailyChatMessageCount, resets w/ receipts at midnight, Pro unlimited). Gate in AskChatManager.send → limit message + CustomPaywallSheet (showLimitPaywall fullScreenCover). Footer under composer (Figma 1643-8075): disclaimer + "N messages left today", 10pt medium foregroundSecondary centered; composer bottom pad 10.
- **stat cards** (Figma 1643-7904): value medium 50; soft-validated vs rows total; semantic trend colors.
- **Comparison pair** (Figma 1643-7958): chart w/ EXACTLY 2 items renders side-by-side cards (label caps / value medium 20 + trend arrow + % medium 14 / vertical bar on grey track h118, gap title→value 12, value→bar 22); colors = palette[0]/[4].
- **compare_spending tool**: periods[{label,from,to}]≤12 + category → rows label/amount/count.
- **Chart palette**: NOW 20 VIBRANT mixed tones (amber/blue/violet#7C4DFF/gold/green/... AskChartPalette enum, shared). History: earth 20 → +4 purple-earths → vibrant. Bars white text.
- **Data-card chrome unified**: askDataCard() = white r16 + 4px #F3F5F8 inset2 border (chart/stat/txn cards/pair).
- **question type**: {"type":"text","choices":[..]} → .question; chips (AppButton secondary 3xs) via shared AskSuggestionChipsRow (latest-message-only). **followups** field on table/chart/stat → same chips row under widgets.
- **Word-by-word streaming**: wordQueue drains 1 token/60ms (rawPending buffers split words); pendingFinalText settles after last word; resets/errors flush. AskStreamingWordsView (AskFlowLayout) strips asterisks during stream.
- **Markdown**: AskFormat.markdown — inlineOnly parse + EXPLICIT font runs (semibold16 bold / italic) because SwiftUI intents don't style custom fonts. Applied to settled text + questions. Prompt allows **bold**/*italic*.
- **Hero empty state** (Figma 1645-8122): no seeded welcome; empty thread → centered "👋 How's it going {first}" medium 30 + "Wanna' know anything about your finances?" 16 secondary, spacing 12; chips show when empty. heroName from UserManager (fallback "there").
- **Haptic** on reply (light impact). **Scroll-down btn**: spec'd (pad10, r200, hard shadow y4, border), offset -48 above input.
- **Draft tile tap** → AddTransactionSheet PREFILLED (new prefill init params; native .sheet 0.98 detent matching Home; onSave adds txn + confirms card + ✅ msg). Plus btn = instant confirm. Old inline editor = dead code.
- Earlier: transaction_delete, convert_currency, budgets tool, context trim 20, tap-cards→ReceiptDetailSheet, full-history filters (limit 200).

## Version/infra
- **3.5.1 (build 1)** — user archiving/uploading now; What's-New blurb provided. TestFlight 3.5.0(1) live on phone prior.
- Chat data cleared in sim (ask_chat_v1.json deleted). Feedback fillers wiped from Firestore; seed-upload removed (remote=truth, empty=empty).
- Post composer = original grey pill + send-01. Role tap-trick live. Firestore feedback fully live.

## WATCH: feedback writes on new build need Anonymous provider (enabled ✓) — if TestFlight posting fails, check Auth console + "🔐 Anonymous session" log.

## Remaining
#5 email notify (Vercel+Resend planned; needs go + posts-vs-comments pref). #6 billing (user blocked; receipts STILL DOWN — offered legacy direct-OpenRouter fallback via proxy?). New-chat button (threads immortal). Retry/copy. Receipt-in-chat, voice, category cleanup, line chart, budget stat. App Check enforcement later. Dead-code cleanup (inline draft editor, cancelUpdate/cancelDelete unused, debug row).
