# Savepoint — CashMonki — 2026-07-17 22:40

## Where things stand
App: `/Users/DGC/Desktop/cashooya-playground`. Scheme "Cashooya Playground". Build REQUIRES `CODE_SIGNING_ALLOWED=NO`. Sim `2E00AA1F-CCFF-403A-93AB-0C504E2EA1AD`. DerivedData `CashMonki-gefiakeveutzgnaaobjcdlxkkdxx`. SourceKit cross-file errors = FALSE POSITIVES (ignore). All builds this session SUCCEEDED.

## Git state (IMPORTANT)
- Repo on GitHub `dcardines-design/cashmonki`, default branch `main`.
- **Branch `sync-cleanup-and-dataloss-guardrails`** — commit `5871cf4` — holds ALL today's work (sync cleanup + category fixes + data-loss guardrails). build_dd/ excluded + gitignored. NOT pushed.
- **Currently on branch `firestore-source-of-truth`** (based off 5871cf4) — empty so far; this is where the Firestore migration will be built. NOT started (design-first, awaiting approval).

## What was committed (5871cf4)
Sync cleanup (5 phases): killed TransactionSyncManager (2nd engine); one canonical `syncUID()`; write amplification split (`syncToFirebase`=user-doc-only merge:true, `fullSyncToFirebase`=doc+txns); sync parity (subscriptions/categories/ask-chat→cloud, budgets restore on login via `mergeBudgets`, +7 dropped UserData CodingKeys); per-user tombstones.
Onboarding/wallet fixes: `markAsComplete` bumps numeric gate 16; restore path marks complete; stable default-wallet id + `dedupeAutoDefaultWallets` prunes empty placeholders.
Category root-cause fixes: `stableCategoryID(name:type:)` (MD5) — built-in category ids were random per install; `CategoryData.init` default id now stable. **`fixOrphanedTransactions` made NON-destructive** (was overwriting orphaned txns to "No Category" every launch + syncing the nuke — THE cause of the category data loss). `Txn.categoryId` now `var`.
Data-loss guardrails: `saveCurrentUserLocally` REFUSES to overwrite a box that has transactions with empty/reset data (kills silent-wipe class); rolling safety snapshots (3 slots) `safetySnap_<uid>_<n>` + `restoreFromSafetySnapshot()`.

## The category data-loss incident (context)
User's transactions all showed "No Category" ❓ after cross-install login. Root: (1) built-in category ids random per install → txns orphaned across installs; (2) old `fixOrphanedTransactions` nuked orphaned txns to "No Category" on every launch and synced it to cloud → original category NAME+id destroyed on txn AND in cloud. Category LIST itself is intact (Edit Categories shows all custom cats). Unrecoverable per-txn unless original device has good data.
User HAS the original device with good data. SAFE recovery (not yet done): keep it logged in, Airplane Mode, then force-upload local→cloud OVERWRITE (no merge-in — merge would let poisoned cloud win by newer timestamp). Offered a one-tap "use this device as source of truth" button — deferred into the Firestore migration.

## Firebase console (done via Chrome automation)
- App Check debug tokens registered: `sim-xcode` (F1529067…), `sim-xcode-2` (6609175C…). NOTE token ROTATES on fresh sim install — re-register the one printed at launch.
- Upgraded to Blaze + $10 budget alert (was Spark, quota was exhausted).

## Current direction: migrate to Firestore-as-source-of-truth
User frustrated with the hand-rolled sync (rightly). Decided to migrate. Requirements CONFIRMED both native: (1) keep ANONYMOUS users (no forced Gmail/Apple), (2) OFFLINE add works (Firestore offline persistence).
Ran a full data-type audit (agent) — inventory of every persisted type/key/scope.
**Delivered a design-first migration plan as an ARTIFACT:** https://claude.ai/code/artifact/af206a68-195c-4614-aebc-fcf4f0923e02 (file: scratchpad/firestore-migration-plan.html). Covers: target `users/{uid}/{transactions,accounts,budgets,categories,subscriptions,receiptImages,askChat}/{id}`; data-type map (KEEP/MOVE/DROP); existing-user launch SAFETY CONTRACT (idempotent flag-guarded import `migratedToFirestore_v1`, keep local box+snapshots as rollback, same uid = no re-login, reversible behind flag); 5-phase rollout; deletes ~80% of sync code (boxes, merge fns, tombstones, TransactionSyncManager, claim/connect, guest boxes).
Open decisions in plan §07: ask-chat blob vs docs, receipt images Firestore vs Storage, unify AccountData+SubAccount (color/icon/active currently dropped), security rules `request.auth.uid == uid`.

## Next steps
- AWAIT user approval of the migration plan shape.
- On approval: Phase 1 = enable Firestore offline persistence + read repositories behind a feature flag (non-destructive, parallel to current).
- Still pending from before: RevenueCat transfer behavior → "Transfer to new App User ID" (user-side); Firestore security rules paste (task #12); re-archive 3.6.0 build 3 for TestFlight (none of today's fixes in current TestFlight build); push branches when user asks.

## User prefs (persist)
Caveman mode FULL. Data is PRECIOUS — no destructive auto-mutation; all merges union/non-destructive. Never `sed -i` in-place (use Edit). CLAUDE.md + Info.plist hold secrets (RevenueCat/Facebook/Twitter/OpenAI) — never commit/expose. UI changes need explicit approval. Testing via Xcode Run wipes the sim container each run (explains fresh containers).
