# Savepoint — CashMonki: AI-chat echo fixes + PROD data-loss incident (ACTIVE)

**Date:** 2026-07-08 00:00
**Working in:** `~/Desktop/cashooya-playground` (CashMonki iOS/SwiftUI). Repo `dcardines-design/cashmonki`. **CAVEMAN MODE ACTIVE** (dropped to normal prose for the data-loss part per auto-clarity — irreversible stakes).

## Part A — AI chat echo bug (DONE, uncommitted, not built)
Screenshot showed `[Proposed transaction draft: 100.0 PHP Coffee]` leaking as chat text. Root cause: `AskPage.swift historyText` fed prior draft turns back to the model as a copyable bracket marker; model imitated its own past turn instead of emitting real draft JSON.

**Fixes (3 layers), files:**
- `CashMonki/Pages/AskPage.swift` — reworded ALL `historyText` markers to past-tense app-event annotations; added `.systemNote(String)` Content case (renders identical to `.text`, but history annotation makes it un-echoable); converted 4 completion-outcome appends (`✅ Added`×3, `🗑️ Deleted`) from `.text` → `.systemNote`.
- `CashMonki/Functions/AskChatService.swift` — 2 CRITICAL systemPrompt rules: don't copy parenthetical app records; never write `✅ Added`/`🗑️ Deleted` completion lines (fake-save guard).
- Both `swiftc -parse` clean. Only 2 exhaustive switches over Content (historyText:46, render:755) — both updated. Codable auto-synths new case. Left as `.text`: user msgs, ⚠️ errors, streaming, cancels.
- NOT built/verified. Verify: "add 20 coffees" → repeated Yes; each add shows real tile + no stray marker/✅ text between.

## Part B — PRODUCTION data-loss incident (URGENT, IN PROGRESS)
User: "my transaction history disappeared" after an **App Store update** (shipped v3.3.0, commit 6749963). This is REAL personal data on their physical device, unrelated to Part A.

**Storage facts (all confirmed by reading code):**
- Transactions are **LOCAL-FIRST**: `enableFirebaseSync` defaults **OFF**. Stored as one JSON blob in UserDefaults key `currentUser_firebase_<firebaseUID>` (`UserManager.swift:2209`, save at ~2126 via `saveCurrentUserLocally()`).
- SwiftData schema is only `Item.self` — NOT transactions.
- `Txn` custom decoder (`Models.swift:2040`) IS backward-compatible (all new fields `decodeIfPresent ?? default`; required core: txID, accountID, category, amount, date, createdAt, primaryCurrency).
- **DESTRUCTIVE fallback:** on "no local data" the app creates an empty user and **immediately `saveCurrentUserLocally()` at `UserManager.swift:330`** — overwrites the same key. Fires automatically on first launch.
- `cleanupCrossContaminatedData` (277/333) deletes data whose email ≠ current user email — another wipe suspect.

**User answers:** No backups (no iCloud/Finder). Barely opened app since. **NOT pushed to Firestore** (so NO cloud copy — local only).

**PIVOTAL new info (end of session):** user is now shown as **"Cashmonki User" / not Dante** = the app **lost auth and fell back to guest** (default identity, `UserManager.swift:951/953`). This means firebaseUID CHANGED → empty-save at :330 went to the **guest key, NOT Dante's key** → **Dante's original blob is very likely STILL in UserDefaults under Dante's old `currentUser_firebase_<DanteUID>` key, untouched.** RECOVERABLE.

## Next steps (SAFE recovery order — do NOT reorder)
1. **Do NOT log back in as Dante yet.** Logging in → `loadUserFromLocalStorage(DanteUID)` → if decode FAILS it hits :295→:330 and **overwrites Dante's real blob = permanent loss.**
2. **First extract raw bytes non-destructively:** build a DEBUG-only launch dump that prints every UserDefaults key with prefix `currentUser` + raw JSON string, run on device via Xcode while still guest (writes only touch guest key, Dante's blob safe). Capture console → save Dante's JSON to a file.
3. Decode that JSON offline; if it fails, find the incompatible field (check UserData decoder `Models.swift:1574`, AccountData/`SubAccount` in `Models/AccountModels.swift`) and write a tolerant decoder.
4. Restore: re-save the recovered UserData under the current user's key, or fix auth so Dante's UID loads it once decode is safe.
5. **Root-cause fix to ship:** make the empty-user fallback NON-destructive — never `saveCurrentUserLocally()` over an existing non-empty raw blob without a confirmed successful decode/backup; and fix whatever auth-loss made them drop to guest on update.

## Open
- Need: confirm which ID Firestore/local uses (UserData.id vs firebaseUID) — Firestore user doc = `users/<userData.id.uuidString>` (`FirestoreService.swift:67`) but moot since nothing synced.
- Part A verify + commit still pending.
