# Savepoint — CashMonki — 2026-08-25 17:07 — iCloud data loss + Play Store scoping

**Working in:** `/Users/DGC/Desktop/cashooya-playground` (BROKEN — see below)

Session goal was "launch CashMonki on Play Store". Never got there: the repo is unreadable.
Most of this session was diagnosis and recovery planning.

---

## 1. The repo is destroyed by iCloud — this is the headline

Desktop is iCloud-synced and **"Optimize Mac Storage" is ON** (`defaults read com.apple.bird
optimize-storage` → `1`). Disk is at **94% full** (396 GB used / 26 GB free of 460 GB), which is
what triggered eviction. macOS evicted the repo to placeholders and will not download it back.

State of `/Users/DGC/Desktop/cashooya-playground`:
- **186 of 387 files** under `CashMonki/` are `compressed,dataless` — read as empty.
  `CashMonki/CashMonkiApp.swift` included.
- `CashMonki.xcodeproj/` is **empty**. `CashMonki.xcodeproj.zip` is a dataless stub (`file` → "empty").
- `.git/config` and **every ref** in `.git/refs/{heads,remotes}` are dataless → git cannot resolve
  a branch. This is why every `git` command hangs (2min+ timeouts all session).
- `.git` contains iCloud conflict copies: `index 2`, `index 3`, `index 4.lock`, `index 5.lock`.

Recovery attempts that FAILED: `brctl download` (exit 0, nothing materialized; `brctl status` →
`BRCloudDocsErrorDomain:30 "Client zone not found"`), plain `cat` to force a fault, same with
sandbox disabled. `fileproviderd` logs show it accepting the request and never delivering.
Dataless count stayed at exactly 186 throughout.

**Detect it cheaply:** `ls -lRO <dir> | grep -c dataless` — reads metadata only, does not hang.

### Untested hypothesis worth trying
94% disk may be why downloads never complete — macOS resists rehydrating when space is tight.
Free ~40 GB (get under 85%), then Finder → right-click folder → **Download Now**. Not yet attempted;
user has not freed space.

---

## 2. What was actually lost, and what wasn't

GitHub `dcardines-design/cashmonki` (**public**, 14.5 MB) last pushed **2026-08-07** = the usable
source baseline. `dcardines-design/cashooya-playground` is private but **0 KB / empty** — ignore it.

| Work | Status |
|---|---|
| Aug 8 — 7 commits (Ask analytics, 64 events wired, feedback-board fix, `app_environment` tag, 3.7.1 bump) | Committed locally, never pushed. **Shipped in the 3.7.1 binary**; source lost |
| Aug 9 — Apple Search Ads attribution (`PostHogManager.registerAttribution()` + `CashMonkiApp.swift:547`) | Uncommitted. Lost |
| Aug 10 — hide/show balances toggle | Uncommitted. Lost |
| PostHog dashboards 941658 / 1974766, 55-insight dedupe, filters on 28 tiles | **Safe — on PostHog's servers** |
| ASC 3.7.1 version + What's New | **Safe — on Apple's servers** |
| Built archive `CashMonki 3.7.1 (13).xcarchive` | **Safe** — `~/Library/Developer/Xcode/Archives/2026-08-08/`, outside iCloud |

**3.7.1 IS LIVE on the App Store** — verified via
`curl "https://itunes.apple.com/lookup?id=6755343516"` → version `3.7.1`, released
`2026-08-08T18:42:37Z`. So the Aug 8 work shipped; only its source is gone.

### RECOVERY PATH — do not skip this
**Claude Code transcripts are in `~/.claude/projects/-Users-DGC/`, NOT in iCloud, fully intact.**
They contain every `Edit`/`Write` tool call with full old/new text. The lost code can be *replayed*
onto the Aug 7 baseline, not rewritten. Hours, not days.

- `d47afb02-2875-4c3d-886d-74d60278a0db.jsonl` — Aug 9 session (attribution)
- `e8d40626-30f9-45a4-90e1-5bbb50f0f94b.jsonl` — Aug 10 session (balances toggle; 11 edits to
  `HomePageCharts.swift`, 1 each to `HomePage.swift`, `HomePageHelpers.swift`, `HomePageComponents.swift`)
- `bde7e15b-390d-4834-8dc1-a04accd5739f.jsonl` — Aug 8 session, 9.6 MB (the shipped commits)

The full Aug 10 savepoint text was recovered from `e8d40626` and is reproduced in this session's
transcript — it holds the entire Play Store scoping (measurements, locked decisions, the open
database question). **Re-read it before planning the Android build.**

### Lost feature specs (from the recovered Aug 10 savepoint)
Hide/show balances toggle — user-approved spec: masks big total + all chart numbers as `-₱•••••`
(bullets, sign and currency symbol preserved); recent transactions deliberately NOT masked;
homepage only, `@AppStorage("home_amounts_hidden")`. `eye`/`eye-off` imagesets already exist in
`CashMonki/DesignSystem/Assets.xcassets` (DesignSystem catalog, not the main one).

---

## 3. Prevention — DONE this session

Added `/Users/DGC/.claude/hooks/savepoint-autopush.sh` and registered it in
`~/.claude/settings.json` as `PostToolUse` on `Write|Edit|MultiEdit` (timeout 120).
Backup: `~/.claude/settings.json.bak`.

Any write to `*/savepoints/*.md` → `git add -A` → commit → `push origin <branch>`.
Guards: aborts if **any** file in `.git` is dataless (prevents committing empty files over good
remote history — the exact failure that happened here); every git call runs under a kill watchdog
so dataless files can't hang it; never force-pushes. Disable with `SAVEPOINT_AUTOPUSH=0`.

**Hooks load at startup — this is not active until the next session.**

Still NOT done: moving the repo off iCloud. Auto-push cannot help while git can't read its own refs.

---

## 4. Play Store — where the actual request stands

User asked to launch CashMonki on Google Play, then asked me to do the signup. Told them no on both:
- **No Android app exists.** 80,322 lines of Swift, 0 lines of Kotlin. 61% is SwiftUI needing a
  Compose rewrite. Nothing to upload.
- **Signup requires their identity, ID, payment method, and personal acceptance of the Developer
  Distribution Agreement.** I do not sign into accounts or enter payment details on their behalf.

Advised starting signup anyway, in parallel: identity verification takes days–weeks, and new
**personal** accounts need 12 testers × 14 continuous days of closed testing before production
unlocks (Organization accounts skip this but need a D-U-N-S number). Play rules change often and my
knowledge cutoff is May 2026 — offered to verify current requirements, user has not answered.

---

## Open questions blocking everything

1. **Port the current app as-is, or build the business-tracker variant?** Asked twice, unanswered.
   Aug 10 scoped the *business* app (net profit home, Reports tab, client/vendor, deductible flag,
   tax rate, COGS-vs-opex + 5 new Ask tools). Today the user said "the current app with all its
   features." These are different products.
2. **Hybrid Firebase-Auth+Functions+Supabase-Postgres, vs all-Supabase?** Open since Aug 10.
   Key finding then: the 1,717-line functions codebase touches Firestore in exactly ONE place
   (`firebase-functions/src/index.ts:185`) — it's an OpenRouter proxy, so "reuse the backend" is not
   an argument for keeping Firestore as the ledger.
3. Clone `dcardines-design/cashmonki` → `~/Projects/cashmonki`? Proposed repeatedly, never approved.

## Carried over, still unresolved
- **Paywall: 78 real-user views → 0 trials → 0 subscriptions (30d).** Tracking bug or genuine 0%?
  Highest-value open question in the project. Blocks marketing spend — and doubly so before
  doubling the audience via Android.
- `dcardines-design/cashmonki` is **public** and contains `GoogleService-Info.plist`. Needs an audit.
- PostHog key `~/.posthog_key` missing `cohort:write`, `query:read`, `event_definition:read`;
  its value is in an old transcript. User chose to keep it.
- `UserManager.updateBudget` Firebase sync still commented out (AI budget edits local-only).
- Marketing assets ready but unshipped: 4 demo videos in `~/Documents`,
  `CashMonki_AppStore_Optimization.md` never pushed to ASC.

## Gotchas
- Scheme is **"Cashooya Playground"**, not CashMonki.
- `ls -lRO … | grep -c dataless` is the safe eviction check; `find`/`grep`/`git` hang on this tree.
- macOS has no `timeout` binary — use a background-PID + `sleep`/`kill` watchdog.
- Do **not** `git add -A` in the Desktop repo. 186 files read as empty; committing would record
  deletions and pushing would overwrite the good Aug 7 remote.

## User prefs
Caveman FULL. Data PRECIOUS. AskUserQuestion for choices. UI changes need approval. Never `sed -i`.
No subagents/workflows unless asked. I do not sign into accounts or enter credentials for the user.
