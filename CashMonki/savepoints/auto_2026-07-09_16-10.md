# Savepoint — 2026-07-09 16:10

## Task
CashMonki auth + cloud sync. Login re-enabled, cloud-sync default-on, now iterating on login screen UI.

## Done + verified (builds + runs on sim)
- **Cloud sync default ON** (`UserManager.swift`): 3 `enableFirebaseSync` defaults false→true; `migrateEnableSyncIfNeeded()` (upload-only, never pull); reinstall cloud-restore in `restoreUserSession()` Step 2, **guarded `transactions.isEmpty`** so local never clobbered.
- **Login re-enabled** (`CashMonkiApp.swift`): gate `if authManager.isAuthenticated || authManager.isGuestMode { app } else { AuthenticationView }`. Anonymous no longer counts as authed (`AuthenticationManager.checkAuthenticationStatus` guards `!firebaseUser.isAnonymous`).
- **Guest mode** (`AuthenticationManager`): `isGuestMode` @Published + persisted; `continueAsGuest()` / `exitGuestMode()`; cleared on logout.
- Login gate confirmed live via sim screenshot.

## In flight (this session, build `bty6peckz` running when stopped)
LoginView.swift restructured + AppButton.swift:
1. **Colored brand logos**: AppButton renders `fullColorIconNames = ["google","apple"]` as `.renderingMode(.original)` (was `.template` → purple). Google svg was ALREADY full-color (gradients) — no asset rewrite needed.
2. **"Continue with Email"** button added to socialLoginSection (leftIcon "envelope" SF symbol) → `withAnimation { showEmailForm = true }` reveals formSection.
3. **Email form now disclosure-gated**: `@State showEmailForm=false`; `if showEmailForm { formSection }`.
4. **Guest moved** from formSection bottom → new `guestSection` under the OR divider (hierarchy `.secondary`).
- New body order: header → social(Apple/Google/Email) → (form if revealed) → OR → guest.

## NEXT (immediately on resume)
- [ ] Check build `bty6peckz` result (`/tmp/cm_build4.log`). If pass: reinstall + screenshot, verify all 3 (colored logos, Email reveal, guest under OR).
- [ ] Google svg uses SVG filters/gradients — Xcode imageset SVG support limited; **verify logo actually renders in color** on screenshot, may need simpler flat 4-color G if it renders wrong.
- [ ] Apple logo should render black (its svg) — confirm not blank.

## Build/run recipe (memorize)
- Scheme `Cashooya Playground`, sim iPhone 16 id `C50E212F-BCE1-4953-92C0-8606A7EE94A0`, bundle `com.dante.cashmonki`.
- MUST pass `CODE_SIGNING_ALLOWED=NO` — else RevenueCat bundles fail codesign ("resource fork... detritus"; Desktop is iCloud-synced, xattrs re-added, can't strip).
- Plain `xcodebuild build` w/o that flag exits 0 building only deps (no .app) — misleading; app target fails only on full build.
- `xcodebuild ... -derivedDataPath ./build_dd CODE_SIGNING_ALLOWED=NO build` → install `build_dd/Build/Products/Debug-iphonesimulator/CashMonki.app` → `simctl launch`.

## Open product decisions (not blocking)
- Apple 5.1.1(v): guest button satisfies "no forced signup". Still owe **in-app account delete** (required since signup offered).
- Opt-out key instability (`enableFirebaseSync_<uuid>`) — guest/sync re-enable edge, deferred.
- RegisterView not yet updated (guest/email changes are LoginView only).
- Data-flow end-to-end test still not run (register→add tx→delete→reinstall→login→restored?).

## Note
CLAUDE.md: UI changes need approval — all UI changes here were user-requested (approved).
